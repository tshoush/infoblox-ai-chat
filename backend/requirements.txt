Flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0
requests==2.32.3
python-dotenv==1.0.0
tabulate==0.9.0

# Caching
redis==5.0.1

# RAG / document processing
langchain==0.1.0
langchain-community==0.0.10
tiktoken>=0.7.0
# faiss-cpu (RAG vector store) is optional and lives in requirements-rag.txt —
# it has no wheel on some platforms (RHEL 7 / old glibc). setup.sh installs it
# best-effort; RAG degrades gracefully if it's absent.
PyPDF2==3.0.1
PyYAML==6.0.1
beautifulsoup4==4.12.2
lxml
# numpy pinned <2 so it installs from a wheel on older toolchains (numpy 2.x
# source-builds and needs GCC >= 9.3, which RHEL 7 doesn't ship).
numpy>=1.26,<2
# scikit-learn is optional (not imported by the core) and may not build on old
# glibc — install separately if you need it.
# scikit-learn
# sentence-transformers==3.0.1

# LLM providers
openai==1.12.0
anthropic>=0.40
ollama==0.1.7

# Testing
pytest==7.4.4
pytest-cov==4.1.0
pytest-mock==3.12.0
