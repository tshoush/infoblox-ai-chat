"""
Autogenerated WAPI tools for Infoblox.

This file is automatically generated by the ToolGenerator. Do not edit manually.
"""
import requests
from backend.config import load_config

config = load_config().infoblox
BASE_URL = f"https://{config.grid_ip}/wapi/{config.wapi_version}/"
AUTH = (config.admin_user, config.admin_pass)
VERIFY_SSL = False



def search_ad_auth_service(**kwargs) -> dict:
    """Search for ad_auth_service objects."""
    url = f"{BASE_URL}ad_auth_service"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ad_auth_service(data: dict) -> dict:
    """Create a new ad_auth_service object."""
    url = f"{BASE_URL}ad_auth_service"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ad_auth_service(ref: str, data: dict) -> dict:
    """Update a ad_auth_service object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ad_auth_service(ref: str) -> dict:
    """Delete a ad_auth_service object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_admingroup(**kwargs) -> dict:
    """Search for admingroup objects."""
    url = f"{BASE_URL}admingroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_admingroup(data: dict) -> dict:
    """Create a new admingroup object."""
    url = f"{BASE_URL}admingroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_admingroup(ref: str, data: dict) -> dict:
    """Update a admingroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_admingroup(ref: str) -> dict:
    """Delete a admingroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_adminrole(**kwargs) -> dict:
    """Search for adminrole objects."""
    url = f"{BASE_URL}adminrole"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_adminrole(data: dict) -> dict:
    """Create a new adminrole object."""
    url = f"{BASE_URL}adminrole"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_adminrole(ref: str, data: dict) -> dict:
    """Update a adminrole object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_adminrole(ref: str) -> dict:
    """Delete a adminrole object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_adminuser(**kwargs) -> dict:
    """Search for adminuser objects."""
    url = f"{BASE_URL}adminuser"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_adminuser(data: dict) -> dict:
    """Create a new adminuser object."""
    url = f"{BASE_URL}adminuser"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_adminuser(ref: str, data: dict) -> dict:
    """Update a adminuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_adminuser(ref: str) -> dict:
    """Delete a adminuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_allendpoints(**kwargs) -> dict:
    """Search for allendpoints objects."""
    url = f"{BASE_URL}allendpoints"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_allendpoints(data: dict) -> dict:
    """Create a new allendpoints object."""
    url = f"{BASE_URL}allendpoints"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_allendpoints(ref: str, data: dict) -> dict:
    """Update a allendpoints object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_allendpoints(ref: str) -> dict:
    """Delete a allendpoints object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_allnsgroup(**kwargs) -> dict:
    """Search for allnsgroup objects."""
    url = f"{BASE_URL}allnsgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_allnsgroup(data: dict) -> dict:
    """Create a new allnsgroup object."""
    url = f"{BASE_URL}allnsgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_allnsgroup(ref: str, data: dict) -> dict:
    """Update a allnsgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_allnsgroup(ref: str) -> dict:
    """Delete a allnsgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_allrecords(**kwargs) -> dict:
    """Search for allrecords objects."""
    url = f"{BASE_URL}allrecords"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_allrecords(data: dict) -> dict:
    """Create a new allrecords object."""
    url = f"{BASE_URL}allrecords"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_allrecords(ref: str, data: dict) -> dict:
    """Update a allrecords object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_allrecords(ref: str) -> dict:
    """Delete a allrecords object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_allrpzrecords(**kwargs) -> dict:
    """Search for allrpzrecords objects."""
    url = f"{BASE_URL}allrpzrecords"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_allrpzrecords(data: dict) -> dict:
    """Create a new allrpzrecords object."""
    url = f"{BASE_URL}allrpzrecords"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_allrpzrecords(ref: str, data: dict) -> dict:
    """Update a allrpzrecords object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_allrpzrecords(ref: str) -> dict:
    """Delete a allrpzrecords object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_approvalworkflow(**kwargs) -> dict:
    """Search for approvalworkflow objects."""
    url = f"{BASE_URL}approvalworkflow"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_approvalworkflow(data: dict) -> dict:
    """Create a new approvalworkflow object."""
    url = f"{BASE_URL}approvalworkflow"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_approvalworkflow(ref: str, data: dict) -> dict:
    """Update a approvalworkflow object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_approvalworkflow(ref: str) -> dict:
    """Delete a approvalworkflow object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_authpolicy(**kwargs) -> dict:
    """Search for authpolicy objects."""
    url = f"{BASE_URL}authpolicy"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_authpolicy(data: dict) -> dict:
    """Create a new authpolicy object."""
    url = f"{BASE_URL}authpolicy"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_authpolicy(ref: str, data: dict) -> dict:
    """Update a authpolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_authpolicy(ref: str) -> dict:
    """Delete a authpolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_awsrte53taskgroup(**kwargs) -> dict:
    """Search for awsrte53taskgroup objects."""
    url = f"{BASE_URL}awsrte53taskgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_awsrte53taskgroup(data: dict) -> dict:
    """Create a new awsrte53taskgroup object."""
    url = f"{BASE_URL}awsrte53taskgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_awsrte53taskgroup(ref: str, data: dict) -> dict:
    """Update a awsrte53taskgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_awsrte53taskgroup(ref: str) -> dict:
    """Delete a awsrte53taskgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_awsuser(**kwargs) -> dict:
    """Search for awsuser objects."""
    url = f"{BASE_URL}awsuser"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_awsuser(data: dict) -> dict:
    """Create a new awsuser object."""
    url = f"{BASE_URL}awsuser"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_awsuser(ref: str, data: dict) -> dict:
    """Update a awsuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_awsuser(ref: str) -> dict:
    """Delete a awsuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_bfdtemplate(**kwargs) -> dict:
    """Search for bfdtemplate objects."""
    url = f"{BASE_URL}bfdtemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_bfdtemplate(data: dict) -> dict:
    """Create a new bfdtemplate object."""
    url = f"{BASE_URL}bfdtemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_bfdtemplate(ref: str, data: dict) -> dict:
    """Update a bfdtemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_bfdtemplate(ref: str) -> dict:
    """Delete a bfdtemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_bulkhost(**kwargs) -> dict:
    """Search for bulkhost objects."""
    url = f"{BASE_URL}bulkhost"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_bulkhost(data: dict) -> dict:
    """Create a new bulkhost object."""
    url = f"{BASE_URL}bulkhost"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_bulkhost(ref: str, data: dict) -> dict:
    """Update a bulkhost object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_bulkhost(ref: str) -> dict:
    """Delete a bulkhost object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_bulkhostnametemplate(**kwargs) -> dict:
    """Search for bulkhostnametemplate objects."""
    url = f"{BASE_URL}bulkhostnametemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_bulkhostnametemplate(data: dict) -> dict:
    """Create a new bulkhostnametemplate object."""
    url = f"{BASE_URL}bulkhostnametemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_bulkhostnametemplate(ref: str, data: dict) -> dict:
    """Update a bulkhostnametemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_bulkhostnametemplate(ref: str) -> dict:
    """Delete a bulkhostnametemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_cacertificate(**kwargs) -> dict:
    """Search for cacertificate objects."""
    url = f"{BASE_URL}cacertificate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_cacertificate(data: dict) -> dict:
    """Create a new cacertificate object."""
    url = f"{BASE_URL}cacertificate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_cacertificate(ref: str, data: dict) -> dict:
    """Update a cacertificate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_cacertificate(ref: str) -> dict:
    """Delete a cacertificate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_capacityreport(**kwargs) -> dict:
    """Search for capacityreport objects."""
    url = f"{BASE_URL}capacityreport"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_capacityreport(data: dict) -> dict:
    """Create a new capacityreport object."""
    url = f"{BASE_URL}capacityreport"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_capacityreport(ref: str, data: dict) -> dict:
    """Update a capacityreport object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_capacityreport(ref: str) -> dict:
    """Delete a capacityreport object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_captiveportal(**kwargs) -> dict:
    """Search for captiveportal objects."""
    url = f"{BASE_URL}captiveportal"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_captiveportal(data: dict) -> dict:
    """Create a new captiveportal object."""
    url = f"{BASE_URL}captiveportal"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_captiveportal(ref: str, data: dict) -> dict:
    """Update a captiveportal object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_captiveportal(ref: str) -> dict:
    """Delete a captiveportal object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_certificate_authservice(**kwargs) -> dict:
    """Search for certificate:authservice objects."""
    url = f"{BASE_URL}certificate:authservice"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_certificate_authservice(data: dict) -> dict:
    """Create a new certificate:authservice object."""
    url = f"{BASE_URL}certificate:authservice"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_certificate_authservice(ref: str, data: dict) -> dict:
    """Update a certificate:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_certificate_authservice(ref: str) -> dict:
    """Delete a certificate:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ciscoise_endpoint(**kwargs) -> dict:
    """Search for ciscoise:endpoint objects."""
    url = f"{BASE_URL}ciscoise:endpoint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ciscoise_endpoint(data: dict) -> dict:
    """Create a new ciscoise:endpoint object."""
    url = f"{BASE_URL}ciscoise:endpoint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ciscoise_endpoint(ref: str, data: dict) -> dict:
    """Update a ciscoise:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ciscoise_endpoint(ref: str) -> dict:
    """Delete a ciscoise:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_csvimporttask(**kwargs) -> dict:
    """Search for csvimporttask objects."""
    url = f"{BASE_URL}csvimporttask"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_csvimporttask(data: dict) -> dict:
    """Create a new csvimporttask object."""
    url = f"{BASE_URL}csvimporttask"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_csvimporttask(ref: str, data: dict) -> dict:
    """Update a csvimporttask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_csvimporttask(ref: str) -> dict:
    """Delete a csvimporttask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_datacollectioncluster(**kwargs) -> dict:
    """Search for datacollectioncluster objects."""
    url = f"{BASE_URL}datacollectioncluster"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_datacollectioncluster(data: dict) -> dict:
    """Create a new datacollectioncluster object."""
    url = f"{BASE_URL}datacollectioncluster"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_datacollectioncluster(ref: str, data: dict) -> dict:
    """Update a datacollectioncluster object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_datacollectioncluster(ref: str) -> dict:
    """Delete a datacollectioncluster object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_db_objects(**kwargs) -> dict:
    """Search for db_objects objects."""
    url = f"{BASE_URL}db_objects"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_db_objects(data: dict) -> dict:
    """Create a new db_objects object."""
    url = f"{BASE_URL}db_objects"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_db_objects(ref: str, data: dict) -> dict:
    """Update a db_objects object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_db_objects(ref: str) -> dict:
    """Delete a db_objects object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dbsnapshot(**kwargs) -> dict:
    """Search for dbsnapshot objects."""
    url = f"{BASE_URL}dbsnapshot"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dbsnapshot(data: dict) -> dict:
    """Create a new dbsnapshot object."""
    url = f"{BASE_URL}dbsnapshot"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dbsnapshot(ref: str, data: dict) -> dict:
    """Update a dbsnapshot object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dbsnapshot(ref: str) -> dict:
    """Delete a dbsnapshot object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ddns_principalcluster(**kwargs) -> dict:
    """Search for ddns:principalcluster objects."""
    url = f"{BASE_URL}ddns:principalcluster"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ddns_principalcluster(data: dict) -> dict:
    """Create a new ddns:principalcluster object."""
    url = f"{BASE_URL}ddns:principalcluster"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ddns_principalcluster(ref: str, data: dict) -> dict:
    """Update a ddns:principalcluster object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ddns_principalcluster(ref: str) -> dict:
    """Delete a ddns:principalcluster object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ddns_principalcluster_group(**kwargs) -> dict:
    """Search for ddns:principalcluster:group objects."""
    url = f"{BASE_URL}ddns:principalcluster:group"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ddns_principalcluster_group(data: dict) -> dict:
    """Create a new ddns:principalcluster:group object."""
    url = f"{BASE_URL}ddns:principalcluster:group"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ddns_principalcluster_group(ref: str, data: dict) -> dict:
    """Update a ddns:principalcluster:group object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ddns_principalcluster_group(ref: str) -> dict:
    """Delete a ddns:principalcluster:group object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_deleted_objects(**kwargs) -> dict:
    """Search for deleted_objects objects."""
    url = f"{BASE_URL}deleted_objects"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_deleted_objects(data: dict) -> dict:
    """Create a new deleted_objects object."""
    url = f"{BASE_URL}deleted_objects"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_deleted_objects(ref: str, data: dict) -> dict:
    """Update a deleted_objects object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_deleted_objects(ref: str) -> dict:
    """Delete a deleted_objects object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dhcp_statistics(**kwargs) -> dict:
    """Search for dhcp:statistics objects."""
    url = f"{BASE_URL}dhcp:statistics"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dhcp_statistics(data: dict) -> dict:
    """Create a new dhcp:statistics object."""
    url = f"{BASE_URL}dhcp:statistics"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dhcp_statistics(ref: str, data: dict) -> dict:
    """Update a dhcp:statistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dhcp_statistics(ref: str) -> dict:
    """Delete a dhcp:statistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dhcpfailover(**kwargs) -> dict:
    """Search for dhcpfailover objects."""
    url = f"{BASE_URL}dhcpfailover"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dhcpfailover(data: dict) -> dict:
    """Create a new dhcpfailover object."""
    url = f"{BASE_URL}dhcpfailover"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dhcpfailover(ref: str, data: dict) -> dict:
    """Update a dhcpfailover object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dhcpfailover(ref: str) -> dict:
    """Delete a dhcpfailover object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dhcpoptiondefinition(**kwargs) -> dict:
    """Search for dhcpoptiondefinition objects."""
    url = f"{BASE_URL}dhcpoptiondefinition"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dhcpoptiondefinition(data: dict) -> dict:
    """Create a new dhcpoptiondefinition object."""
    url = f"{BASE_URL}dhcpoptiondefinition"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dhcpoptiondefinition(ref: str, data: dict) -> dict:
    """Update a dhcpoptiondefinition object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dhcpoptiondefinition(ref: str) -> dict:
    """Delete a dhcpoptiondefinition object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dhcpoptionspace(**kwargs) -> dict:
    """Search for dhcpoptionspace objects."""
    url = f"{BASE_URL}dhcpoptionspace"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dhcpoptionspace(data: dict) -> dict:
    """Create a new dhcpoptionspace object."""
    url = f"{BASE_URL}dhcpoptionspace"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dhcpoptionspace(ref: str, data: dict) -> dict:
    """Update a dhcpoptionspace object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dhcpoptionspace(ref: str) -> dict:
    """Delete a dhcpoptionspace object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery(**kwargs) -> dict:
    """Search for discovery objects."""
    url = f"{BASE_URL}discovery"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery(data: dict) -> dict:
    """Create a new discovery object."""
    url = f"{BASE_URL}discovery"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery(ref: str, data: dict) -> dict:
    """Update a discovery object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery(ref: str) -> dict:
    """Delete a discovery object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_credentialgroup(**kwargs) -> dict:
    """Search for discovery:credentialgroup objects."""
    url = f"{BASE_URL}discovery:credentialgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_credentialgroup(data: dict) -> dict:
    """Create a new discovery:credentialgroup object."""
    url = f"{BASE_URL}discovery:credentialgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_credentialgroup(ref: str, data: dict) -> dict:
    """Update a discovery:credentialgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_credentialgroup(ref: str) -> dict:
    """Delete a discovery:credentialgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_device(**kwargs) -> dict:
    """Search for discovery:device objects."""
    url = f"{BASE_URL}discovery:device"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_device(data: dict) -> dict:
    """Create a new discovery:device object."""
    url = f"{BASE_URL}discovery:device"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_device(ref: str, data: dict) -> dict:
    """Update a discovery:device object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_device(ref: str) -> dict:
    """Delete a discovery:device object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_devicecomponent(**kwargs) -> dict:
    """Search for discovery:devicecomponent objects."""
    url = f"{BASE_URL}discovery:devicecomponent"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_devicecomponent(data: dict) -> dict:
    """Create a new discovery:devicecomponent object."""
    url = f"{BASE_URL}discovery:devicecomponent"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_devicecomponent(ref: str, data: dict) -> dict:
    """Update a discovery:devicecomponent object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_devicecomponent(ref: str) -> dict:
    """Delete a discovery:devicecomponent object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_deviceinterface(**kwargs) -> dict:
    """Search for discovery:deviceinterface objects."""
    url = f"{BASE_URL}discovery:deviceinterface"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_deviceinterface(data: dict) -> dict:
    """Create a new discovery:deviceinterface object."""
    url = f"{BASE_URL}discovery:deviceinterface"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_deviceinterface(ref: str, data: dict) -> dict:
    """Update a discovery:deviceinterface object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_deviceinterface(ref: str) -> dict:
    """Delete a discovery:deviceinterface object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_deviceneighbor(**kwargs) -> dict:
    """Search for discovery:deviceneighbor objects."""
    url = f"{BASE_URL}discovery:deviceneighbor"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_deviceneighbor(data: dict) -> dict:
    """Create a new discovery:deviceneighbor object."""
    url = f"{BASE_URL}discovery:deviceneighbor"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_deviceneighbor(ref: str, data: dict) -> dict:
    """Update a discovery:deviceneighbor object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_deviceneighbor(ref: str) -> dict:
    """Delete a discovery:deviceneighbor object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_devicesupportbundle(**kwargs) -> dict:
    """Search for discovery:devicesupportbundle objects."""
    url = f"{BASE_URL}discovery:devicesupportbundle"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_devicesupportbundle(data: dict) -> dict:
    """Create a new discovery:devicesupportbundle object."""
    url = f"{BASE_URL}discovery:devicesupportbundle"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_devicesupportbundle(ref: str, data: dict) -> dict:
    """Update a discovery:devicesupportbundle object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_devicesupportbundle(ref: str) -> dict:
    """Delete a discovery:devicesupportbundle object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_diagnostictask(**kwargs) -> dict:
    """Search for discovery:diagnostictask objects."""
    url = f"{BASE_URL}discovery:diagnostictask"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_diagnostictask(data: dict) -> dict:
    """Create a new discovery:diagnostictask object."""
    url = f"{BASE_URL}discovery:diagnostictask"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_diagnostictask(ref: str, data: dict) -> dict:
    """Update a discovery:diagnostictask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_diagnostictask(ref: str) -> dict:
    """Delete a discovery:diagnostictask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_gridproperties(**kwargs) -> dict:
    """Search for discovery:gridproperties objects."""
    url = f"{BASE_URL}discovery:gridproperties"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_gridproperties(data: dict) -> dict:
    """Create a new discovery:gridproperties object."""
    url = f"{BASE_URL}discovery:gridproperties"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_gridproperties(ref: str, data: dict) -> dict:
    """Update a discovery:gridproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_gridproperties(ref: str) -> dict:
    """Delete a discovery:gridproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_memberproperties(**kwargs) -> dict:
    """Search for discovery:memberproperties objects."""
    url = f"{BASE_URL}discovery:memberproperties"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_memberproperties(data: dict) -> dict:
    """Create a new discovery:memberproperties object."""
    url = f"{BASE_URL}discovery:memberproperties"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_memberproperties(ref: str, data: dict) -> dict:
    """Update a discovery:memberproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_memberproperties(ref: str) -> dict:
    """Delete a discovery:memberproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_sdnnetwork(**kwargs) -> dict:
    """Search for discovery:sdnnetwork objects."""
    url = f"{BASE_URL}discovery:sdnnetwork"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_sdnnetwork(data: dict) -> dict:
    """Create a new discovery:sdnnetwork object."""
    url = f"{BASE_URL}discovery:sdnnetwork"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_sdnnetwork(ref: str, data: dict) -> dict:
    """Update a discovery:sdnnetwork object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_sdnnetwork(ref: str) -> dict:
    """Delete a discovery:sdnnetwork object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_status(**kwargs) -> dict:
    """Search for discovery:status objects."""
    url = f"{BASE_URL}discovery:status"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_status(data: dict) -> dict:
    """Create a new discovery:status object."""
    url = f"{BASE_URL}discovery:status"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_status(ref: str, data: dict) -> dict:
    """Update a discovery:status object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_status(ref: str) -> dict:
    """Delete a discovery:status object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discovery_vrf(**kwargs) -> dict:
    """Search for discovery:vrf objects."""
    url = f"{BASE_URL}discovery:vrf"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discovery_vrf(data: dict) -> dict:
    """Create a new discovery:vrf object."""
    url = f"{BASE_URL}discovery:vrf"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discovery_vrf(ref: str, data: dict) -> dict:
    """Update a discovery:vrf object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discovery_vrf(ref: str) -> dict:
    """Delete a discovery:vrf object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_discoverytask(**kwargs) -> dict:
    """Search for discoverytask objects."""
    url = f"{BASE_URL}discoverytask"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_discoverytask(data: dict) -> dict:
    """Create a new discoverytask object."""
    url = f"{BASE_URL}discoverytask"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_discoverytask(ref: str, data: dict) -> dict:
    """Update a discoverytask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_discoverytask(ref: str) -> dict:
    """Delete a discoverytask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_distributionschedule(**kwargs) -> dict:
    """Search for distributionschedule objects."""
    url = f"{BASE_URL}distributionschedule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_distributionschedule(data: dict) -> dict:
    """Create a new distributionschedule object."""
    url = f"{BASE_URL}distributionschedule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_distributionschedule(ref: str, data: dict) -> dict:
    """Update a distributionschedule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_distributionschedule(ref: str) -> dict:
    """Delete a distributionschedule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dns64group(**kwargs) -> dict:
    """Search for dns64group objects."""
    url = f"{BASE_URL}dns64group"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dns64group(data: dict) -> dict:
    """Create a new dns64group object."""
    url = f"{BASE_URL}dns64group"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dns64group(ref: str, data: dict) -> dict:
    """Update a dns64group object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dns64group(ref: str) -> dict:
    """Delete a dns64group object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc(**kwargs) -> dict:
    """Search for dtc objects."""
    url = f"{BASE_URL}dtc"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc(data: dict) -> dict:
    """Create a new dtc object."""
    url = f"{BASE_URL}dtc"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc(ref: str, data: dict) -> dict:
    """Update a dtc object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc(ref: str) -> dict:
    """Delete a dtc object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_allrecords(**kwargs) -> dict:
    """Search for dtc:allrecords objects."""
    url = f"{BASE_URL}dtc:allrecords"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_allrecords(data: dict) -> dict:
    """Create a new dtc:allrecords object."""
    url = f"{BASE_URL}dtc:allrecords"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_allrecords(ref: str, data: dict) -> dict:
    """Update a dtc:allrecords object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_allrecords(ref: str) -> dict:
    """Delete a dtc:allrecords object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_certificate(**kwargs) -> dict:
    """Search for dtc:certificate objects."""
    url = f"{BASE_URL}dtc:certificate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_certificate(data: dict) -> dict:
    """Create a new dtc:certificate object."""
    url = f"{BASE_URL}dtc:certificate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_certificate(ref: str, data: dict) -> dict:
    """Update a dtc:certificate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_certificate(ref: str) -> dict:
    """Delete a dtc:certificate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_lbdn(**kwargs) -> dict:
    """Search for dtc:lbdn objects."""
    url = f"{BASE_URL}dtc:lbdn"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_lbdn(data: dict) -> dict:
    """Create a new dtc:lbdn object."""
    url = f"{BASE_URL}dtc:lbdn"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_lbdn(ref: str, data: dict) -> dict:
    """Update a dtc:lbdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_lbdn(ref: str) -> dict:
    """Delete a dtc:lbdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor(**kwargs) -> dict:
    """Search for dtc:monitor objects."""
    url = f"{BASE_URL}dtc:monitor"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor(data: dict) -> dict:
    """Create a new dtc:monitor object."""
    url = f"{BASE_URL}dtc:monitor"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor(ref: str, data: dict) -> dict:
    """Update a dtc:monitor object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor(ref: str) -> dict:
    """Delete a dtc:monitor object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor_http(**kwargs) -> dict:
    """Search for dtc:monitor:http objects."""
    url = f"{BASE_URL}dtc:monitor:http"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor_http(data: dict) -> dict:
    """Create a new dtc:monitor:http object."""
    url = f"{BASE_URL}dtc:monitor:http"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor_http(ref: str, data: dict) -> dict:
    """Update a dtc:monitor:http object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor_http(ref: str) -> dict:
    """Delete a dtc:monitor:http object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor_icmp(**kwargs) -> dict:
    """Search for dtc:monitor:icmp objects."""
    url = f"{BASE_URL}dtc:monitor:icmp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor_icmp(data: dict) -> dict:
    """Create a new dtc:monitor:icmp object."""
    url = f"{BASE_URL}dtc:monitor:icmp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor_icmp(ref: str, data: dict) -> dict:
    """Update a dtc:monitor:icmp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor_icmp(ref: str) -> dict:
    """Delete a dtc:monitor:icmp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor_pdp(**kwargs) -> dict:
    """Search for dtc:monitor:pdp objects."""
    url = f"{BASE_URL}dtc:monitor:pdp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor_pdp(data: dict) -> dict:
    """Create a new dtc:monitor:pdp object."""
    url = f"{BASE_URL}dtc:monitor:pdp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor_pdp(ref: str, data: dict) -> dict:
    """Update a dtc:monitor:pdp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor_pdp(ref: str) -> dict:
    """Delete a dtc:monitor:pdp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor_sip(**kwargs) -> dict:
    """Search for dtc:monitor:sip objects."""
    url = f"{BASE_URL}dtc:monitor:sip"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor_sip(data: dict) -> dict:
    """Create a new dtc:monitor:sip object."""
    url = f"{BASE_URL}dtc:monitor:sip"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor_sip(ref: str, data: dict) -> dict:
    """Update a dtc:monitor:sip object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor_sip(ref: str) -> dict:
    """Delete a dtc:monitor:sip object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor_snmp(**kwargs) -> dict:
    """Search for dtc:monitor:snmp objects."""
    url = f"{BASE_URL}dtc:monitor:snmp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor_snmp(data: dict) -> dict:
    """Create a new dtc:monitor:snmp object."""
    url = f"{BASE_URL}dtc:monitor:snmp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor_snmp(ref: str, data: dict) -> dict:
    """Update a dtc:monitor:snmp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor_snmp(ref: str) -> dict:
    """Delete a dtc:monitor:snmp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_monitor_tcp(**kwargs) -> dict:
    """Search for dtc:monitor:tcp objects."""
    url = f"{BASE_URL}dtc:monitor:tcp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_monitor_tcp(data: dict) -> dict:
    """Create a new dtc:monitor:tcp object."""
    url = f"{BASE_URL}dtc:monitor:tcp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_monitor_tcp(ref: str, data: dict) -> dict:
    """Update a dtc:monitor:tcp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_monitor_tcp(ref: str) -> dict:
    """Delete a dtc:monitor:tcp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_object(**kwargs) -> dict:
    """Search for dtc:object objects."""
    url = f"{BASE_URL}dtc:object"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_object(data: dict) -> dict:
    """Create a new dtc:object object."""
    url = f"{BASE_URL}dtc:object"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_object(ref: str, data: dict) -> dict:
    """Update a dtc:object object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_object(ref: str) -> dict:
    """Delete a dtc:object object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_pool(**kwargs) -> dict:
    """Search for dtc:pool objects."""
    url = f"{BASE_URL}dtc:pool"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_pool(data: dict) -> dict:
    """Create a new dtc:pool object."""
    url = f"{BASE_URL}dtc:pool"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_pool(ref: str, data: dict) -> dict:
    """Update a dtc:pool object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_pool(ref: str) -> dict:
    """Delete a dtc:pool object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_record_a(**kwargs) -> dict:
    """Search for dtc:record:a objects."""
    url = f"{BASE_URL}dtc:record:a"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_record_a(data: dict) -> dict:
    """Create a new dtc:record:a object."""
    url = f"{BASE_URL}dtc:record:a"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_record_a(ref: str, data: dict) -> dict:
    """Update a dtc:record:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_record_a(ref: str) -> dict:
    """Delete a dtc:record:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_record_aaaa(**kwargs) -> dict:
    """Search for dtc:record:aaaa objects."""
    url = f"{BASE_URL}dtc:record:aaaa"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_record_aaaa(data: dict) -> dict:
    """Create a new dtc:record:aaaa object."""
    url = f"{BASE_URL}dtc:record:aaaa"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_record_aaaa(ref: str, data: dict) -> dict:
    """Update a dtc:record:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_record_aaaa(ref: str) -> dict:
    """Delete a dtc:record:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_record_cname(**kwargs) -> dict:
    """Search for dtc:record:cname objects."""
    url = f"{BASE_URL}dtc:record:cname"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_record_cname(data: dict) -> dict:
    """Create a new dtc:record:cname object."""
    url = f"{BASE_URL}dtc:record:cname"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_record_cname(ref: str, data: dict) -> dict:
    """Update a dtc:record:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_record_cname(ref: str) -> dict:
    """Delete a dtc:record:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_record_naptr(**kwargs) -> dict:
    """Search for dtc:record:naptr objects."""
    url = f"{BASE_URL}dtc:record:naptr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_record_naptr(data: dict) -> dict:
    """Create a new dtc:record:naptr object."""
    url = f"{BASE_URL}dtc:record:naptr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_record_naptr(ref: str, data: dict) -> dict:
    """Update a dtc:record:naptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_record_naptr(ref: str) -> dict:
    """Delete a dtc:record:naptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_record_srv(**kwargs) -> dict:
    """Search for dtc:record:srv objects."""
    url = f"{BASE_URL}dtc:record:srv"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_record_srv(data: dict) -> dict:
    """Create a new dtc:record:srv object."""
    url = f"{BASE_URL}dtc:record:srv"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_record_srv(ref: str, data: dict) -> dict:
    """Update a dtc:record:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_record_srv(ref: str) -> dict:
    """Delete a dtc:record:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_server(**kwargs) -> dict:
    """Search for dtc:server objects."""
    url = f"{BASE_URL}dtc:server"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_server(data: dict) -> dict:
    """Create a new dtc:server object."""
    url = f"{BASE_URL}dtc:server"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_server(ref: str, data: dict) -> dict:
    """Update a dtc:server object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_server(ref: str) -> dict:
    """Delete a dtc:server object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_topology(**kwargs) -> dict:
    """Search for dtc:topology objects."""
    url = f"{BASE_URL}dtc:topology"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_topology(data: dict) -> dict:
    """Create a new dtc:topology object."""
    url = f"{BASE_URL}dtc:topology"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_topology(ref: str, data: dict) -> dict:
    """Update a dtc:topology object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_topology(ref: str) -> dict:
    """Delete a dtc:topology object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_topology_label(**kwargs) -> dict:
    """Search for dtc:topology:label objects."""
    url = f"{BASE_URL}dtc:topology:label"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_topology_label(data: dict) -> dict:
    """Create a new dtc:topology:label object."""
    url = f"{BASE_URL}dtc:topology:label"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_topology_label(ref: str, data: dict) -> dict:
    """Update a dtc:topology:label object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_topology_label(ref: str) -> dict:
    """Delete a dtc:topology:label object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dtc_topology_rule(**kwargs) -> dict:
    """Search for dtc:topology:rule objects."""
    url = f"{BASE_URL}dtc:topology:rule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dtc_topology_rule(data: dict) -> dict:
    """Create a new dtc:topology:rule object."""
    url = f"{BASE_URL}dtc:topology:rule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dtc_topology_rule(ref: str, data: dict) -> dict:
    """Update a dtc:topology:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dtc_topology_rule(ref: str) -> dict:
    """Delete a dtc:topology:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_dxl_endpoint(**kwargs) -> dict:
    """Search for dxl:endpoint objects."""
    url = f"{BASE_URL}dxl:endpoint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_dxl_endpoint(data: dict) -> dict:
    """Create a new dxl:endpoint object."""
    url = f"{BASE_URL}dxl:endpoint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_dxl_endpoint(ref: str, data: dict) -> dict:
    """Update a dxl:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_dxl_endpoint(ref: str) -> dict:
    """Delete a dxl:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_extensibleattributedef(**kwargs) -> dict:
    """Search for extensibleattributedef objects."""
    url = f"{BASE_URL}extensibleattributedef"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_extensibleattributedef(data: dict) -> dict:
    """Create a new extensibleattributedef object."""
    url = f"{BASE_URL}extensibleattributedef"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_extensibleattributedef(ref: str, data: dict) -> dict:
    """Update a extensibleattributedef object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_extensibleattributedef(ref: str) -> dict:
    """Delete a extensibleattributedef object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_fileop(**kwargs) -> dict:
    """Search for fileop objects."""
    url = f"{BASE_URL}fileop"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_fileop(data: dict) -> dict:
    """Create a new fileop object."""
    url = f"{BASE_URL}fileop"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_fileop(ref: str, data: dict) -> dict:
    """Update a fileop object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_fileop(ref: str) -> dict:
    """Delete a fileop object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_filterfingerprint(**kwargs) -> dict:
    """Search for filterfingerprint objects."""
    url = f"{BASE_URL}filterfingerprint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_filterfingerprint(data: dict) -> dict:
    """Create a new filterfingerprint object."""
    url = f"{BASE_URL}filterfingerprint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_filterfingerprint(ref: str, data: dict) -> dict:
    """Update a filterfingerprint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_filterfingerprint(ref: str) -> dict:
    """Delete a filterfingerprint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_filtermac(**kwargs) -> dict:
    """Search for filtermac objects."""
    url = f"{BASE_URL}filtermac"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_filtermac(data: dict) -> dict:
    """Create a new filtermac object."""
    url = f"{BASE_URL}filtermac"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_filtermac(ref: str, data: dict) -> dict:
    """Update a filtermac object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_filtermac(ref: str) -> dict:
    """Delete a filtermac object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_filternac(**kwargs) -> dict:
    """Search for filternac objects."""
    url = f"{BASE_URL}filternac"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_filternac(data: dict) -> dict:
    """Create a new filternac object."""
    url = f"{BASE_URL}filternac"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_filternac(ref: str, data: dict) -> dict:
    """Update a filternac object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_filternac(ref: str) -> dict:
    """Delete a filternac object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_filteroption(**kwargs) -> dict:
    """Search for filteroption objects."""
    url = f"{BASE_URL}filteroption"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_filteroption(data: dict) -> dict:
    """Create a new filteroption object."""
    url = f"{BASE_URL}filteroption"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_filteroption(ref: str, data: dict) -> dict:
    """Update a filteroption object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_filteroption(ref: str) -> dict:
    """Delete a filteroption object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_filterrelayagent(**kwargs) -> dict:
    """Search for filterrelayagent objects."""
    url = f"{BASE_URL}filterrelayagent"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_filterrelayagent(data: dict) -> dict:
    """Create a new filterrelayagent object."""
    url = f"{BASE_URL}filterrelayagent"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_filterrelayagent(ref: str, data: dict) -> dict:
    """Update a filterrelayagent object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_filterrelayagent(ref: str) -> dict:
    """Delete a filterrelayagent object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_fingerprint(**kwargs) -> dict:
    """Search for fingerprint objects."""
    url = f"{BASE_URL}fingerprint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_fingerprint(data: dict) -> dict:
    """Create a new fingerprint object."""
    url = f"{BASE_URL}fingerprint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_fingerprint(ref: str, data: dict) -> dict:
    """Update a fingerprint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_fingerprint(ref: str) -> dict:
    """Delete a fingerprint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_fixedaddress(**kwargs) -> dict:
    """Search for fixedaddress objects."""
    url = f"{BASE_URL}fixedaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_fixedaddress(data: dict) -> dict:
    """Create a new fixedaddress object."""
    url = f"{BASE_URL}fixedaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_fixedaddress(ref: str, data: dict) -> dict:
    """Update a fixedaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_fixedaddress(ref: str) -> dict:
    """Delete a fixedaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_fixedaddresstemplate(**kwargs) -> dict:
    """Search for fixedaddresstemplate objects."""
    url = f"{BASE_URL}fixedaddresstemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_fixedaddresstemplate(data: dict) -> dict:
    """Create a new fixedaddresstemplate object."""
    url = f"{BASE_URL}fixedaddresstemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_fixedaddresstemplate(ref: str, data: dict) -> dict:
    """Update a fixedaddresstemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_fixedaddresstemplate(ref: str) -> dict:
    """Delete a fixedaddresstemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ftpuser(**kwargs) -> dict:
    """Search for ftpuser objects."""
    url = f"{BASE_URL}ftpuser"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ftpuser(data: dict) -> dict:
    """Create a new ftpuser object."""
    url = f"{BASE_URL}ftpuser"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ftpuser(ref: str, data: dict) -> dict:
    """Update a ftpuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ftpuser(ref: str) -> dict:
    """Delete a ftpuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_gmcgroup(**kwargs) -> dict:
    """Search for gmcgroup objects."""
    url = f"{BASE_URL}gmcgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_gmcgroup(data: dict) -> dict:
    """Create a new gmcgroup object."""
    url = f"{BASE_URL}gmcgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_gmcgroup(ref: str, data: dict) -> dict:
    """Update a gmcgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_gmcgroup(ref: str) -> dict:
    """Delete a gmcgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_gmcschedule(**kwargs) -> dict:
    """Search for gmcschedule objects."""
    url = f"{BASE_URL}gmcschedule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_gmcschedule(data: dict) -> dict:
    """Create a new gmcschedule object."""
    url = f"{BASE_URL}gmcschedule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_gmcschedule(ref: str, data: dict) -> dict:
    """Update a gmcschedule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_gmcschedule(ref: str) -> dict:
    """Delete a gmcschedule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid(**kwargs) -> dict:
    """Search for grid objects."""
    url = f"{BASE_URL}grid"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid(data: dict) -> dict:
    """Create a new grid object."""
    url = f"{BASE_URL}grid"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid(ref: str, data: dict) -> dict:
    """Update a grid object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid(ref: str) -> dict:
    """Delete a grid object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_cloudapi(**kwargs) -> dict:
    """Search for grid:cloudapi objects."""
    url = f"{BASE_URL}grid:cloudapi"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_cloudapi(data: dict) -> dict:
    """Create a new grid:cloudapi object."""
    url = f"{BASE_URL}grid:cloudapi"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_cloudapi(ref: str, data: dict) -> dict:
    """Update a grid:cloudapi object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_cloudapi(ref: str) -> dict:
    """Delete a grid:cloudapi object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_cloudapi_cloudstatistics(**kwargs) -> dict:
    """Search for grid:cloudapi:cloudstatistics objects."""
    url = f"{BASE_URL}grid:cloudapi:cloudstatistics"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_cloudapi_cloudstatistics(data: dict) -> dict:
    """Create a new grid:cloudapi:cloudstatistics object."""
    url = f"{BASE_URL}grid:cloudapi:cloudstatistics"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_cloudapi_cloudstatistics(ref: str, data: dict) -> dict:
    """Update a grid:cloudapi:cloudstatistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_cloudapi_cloudstatistics(ref: str) -> dict:
    """Delete a grid:cloudapi:cloudstatistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_cloudapi_tenant(**kwargs) -> dict:
    """Search for grid:cloudapi:tenant objects."""
    url = f"{BASE_URL}grid:cloudapi:tenant"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_cloudapi_tenant(data: dict) -> dict:
    """Create a new grid:cloudapi:tenant object."""
    url = f"{BASE_URL}grid:cloudapi:tenant"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_cloudapi_tenant(ref: str, data: dict) -> dict:
    """Update a grid:cloudapi:tenant object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_cloudapi_tenant(ref: str) -> dict:
    """Delete a grid:cloudapi:tenant object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_cloudapi_vm(**kwargs) -> dict:
    """Search for grid:cloudapi:vm objects."""
    url = f"{BASE_URL}grid:cloudapi:vm"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_cloudapi_vm(data: dict) -> dict:
    """Create a new grid:cloudapi:vm object."""
    url = f"{BASE_URL}grid:cloudapi:vm"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_cloudapi_vm(ref: str, data: dict) -> dict:
    """Update a grid:cloudapi:vm object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_cloudapi_vm(ref: str) -> dict:
    """Delete a grid:cloudapi:vm object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_cloudapi_vmaddress(**kwargs) -> dict:
    """Search for grid:cloudapi:vmaddress objects."""
    url = f"{BASE_URL}grid:cloudapi:vmaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_cloudapi_vmaddress(data: dict) -> dict:
    """Create a new grid:cloudapi:vmaddress object."""
    url = f"{BASE_URL}grid:cloudapi:vmaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_cloudapi_vmaddress(ref: str, data: dict) -> dict:
    """Update a grid:cloudapi:vmaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_cloudapi_vmaddress(ref: str) -> dict:
    """Delete a grid:cloudapi:vmaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_dashboard(**kwargs) -> dict:
    """Search for grid:dashboard objects."""
    url = f"{BASE_URL}grid:dashboard"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_dashboard(data: dict) -> dict:
    """Create a new grid:dashboard object."""
    url = f"{BASE_URL}grid:dashboard"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_dashboard(ref: str, data: dict) -> dict:
    """Update a grid:dashboard object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_dashboard(ref: str) -> dict:
    """Delete a grid:dashboard object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_dhcpproperties(**kwargs) -> dict:
    """Search for grid:dhcpproperties objects."""
    url = f"{BASE_URL}grid:dhcpproperties"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_dhcpproperties(data: dict) -> dict:
    """Create a new grid:dhcpproperties object."""
    url = f"{BASE_URL}grid:dhcpproperties"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_dhcpproperties(ref: str, data: dict) -> dict:
    """Update a grid:dhcpproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_dhcpproperties(ref: str) -> dict:
    """Delete a grid:dhcpproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_dns(**kwargs) -> dict:
    """Search for grid:dns objects."""
    url = f"{BASE_URL}grid:dns"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_dns(data: dict) -> dict:
    """Create a new grid:dns object."""
    url = f"{BASE_URL}grid:dns"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_dns(ref: str, data: dict) -> dict:
    """Update a grid:dns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_dns(ref: str) -> dict:
    """Delete a grid:dns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_filedistribution(**kwargs) -> dict:
    """Search for grid:filedistribution objects."""
    url = f"{BASE_URL}grid:filedistribution"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_filedistribution(data: dict) -> dict:
    """Create a new grid:filedistribution object."""
    url = f"{BASE_URL}grid:filedistribution"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_filedistribution(ref: str, data: dict) -> dict:
    """Update a grid:filedistribution object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_filedistribution(ref: str) -> dict:
    """Delete a grid:filedistribution object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_license_pool(**kwargs) -> dict:
    """Search for grid:license_pool objects."""
    url = f"{BASE_URL}grid:license_pool"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_license_pool(data: dict) -> dict:
    """Create a new grid:license_pool object."""
    url = f"{BASE_URL}grid:license_pool"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_license_pool(ref: str, data: dict) -> dict:
    """Update a grid:license_pool object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_license_pool(ref: str) -> dict:
    """Delete a grid:license_pool object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_license_pool_container(**kwargs) -> dict:
    """Search for grid:license_pool_container objects."""
    url = f"{BASE_URL}grid:license_pool_container"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_license_pool_container(data: dict) -> dict:
    """Create a new grid:license_pool_container object."""
    url = f"{BASE_URL}grid:license_pool_container"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_license_pool_container(ref: str, data: dict) -> dict:
    """Update a grid:license_pool_container object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_license_pool_container(ref: str) -> dict:
    """Delete a grid:license_pool_container object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_maxminddbinfo(**kwargs) -> dict:
    """Search for grid:maxminddbinfo objects."""
    url = f"{BASE_URL}grid:maxminddbinfo"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_maxminddbinfo(data: dict) -> dict:
    """Create a new grid:maxminddbinfo object."""
    url = f"{BASE_URL}grid:maxminddbinfo"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_maxminddbinfo(ref: str, data: dict) -> dict:
    """Update a grid:maxminddbinfo object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_maxminddbinfo(ref: str) -> dict:
    """Delete a grid:maxminddbinfo object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_member_cloudapi(**kwargs) -> dict:
    """Search for grid:member:cloudapi objects."""
    url = f"{BASE_URL}grid:member:cloudapi"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_member_cloudapi(data: dict) -> dict:
    """Create a new grid:member:cloudapi object."""
    url = f"{BASE_URL}grid:member:cloudapi"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_member_cloudapi(ref: str, data: dict) -> dict:
    """Update a grid:member:cloudapi object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_member_cloudapi(ref: str) -> dict:
    """Delete a grid:member:cloudapi object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_servicerestart_group(**kwargs) -> dict:
    """Search for grid:servicerestart:group objects."""
    url = f"{BASE_URL}grid:servicerestart:group"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_servicerestart_group(data: dict) -> dict:
    """Create a new grid:servicerestart:group object."""
    url = f"{BASE_URL}grid:servicerestart:group"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_servicerestart_group(ref: str, data: dict) -> dict:
    """Update a grid:servicerestart:group object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_servicerestart_group(ref: str) -> dict:
    """Delete a grid:servicerestart:group object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_servicerestart_group_order(**kwargs) -> dict:
    """Search for grid:servicerestart:group:order objects."""
    url = f"{BASE_URL}grid:servicerestart:group:order"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_servicerestart_group_order(data: dict) -> dict:
    """Create a new grid:servicerestart:group:order object."""
    url = f"{BASE_URL}grid:servicerestart:group:order"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_servicerestart_group_order(ref: str, data: dict) -> dict:
    """Update a grid:servicerestart:group:order object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_servicerestart_group_order(ref: str) -> dict:
    """Delete a grid:servicerestart:group:order object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_servicerestart_request(**kwargs) -> dict:
    """Search for grid:servicerestart:request objects."""
    url = f"{BASE_URL}grid:servicerestart:request"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_servicerestart_request(data: dict) -> dict:
    """Create a new grid:servicerestart:request object."""
    url = f"{BASE_URL}grid:servicerestart:request"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_servicerestart_request(ref: str, data: dict) -> dict:
    """Update a grid:servicerestart:request object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_servicerestart_request(ref: str) -> dict:
    """Delete a grid:servicerestart:request object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_servicerestart_request_changedobject(**kwargs) -> dict:
    """Search for grid:servicerestart:request:changedobject objects."""
    url = f"{BASE_URL}grid:servicerestart:request:changedobject"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_servicerestart_request_changedobject(data: dict) -> dict:
    """Create a new grid:servicerestart:request:changedobject object."""
    url = f"{BASE_URL}grid:servicerestart:request:changedobject"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_servicerestart_request_changedobject(ref: str, data: dict) -> dict:
    """Update a grid:servicerestart:request:changedobject object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_servicerestart_request_changedobject(ref: str) -> dict:
    """Delete a grid:servicerestart:request:changedobject object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_servicerestart_status(**kwargs) -> dict:
    """Search for grid:servicerestart:status objects."""
    url = f"{BASE_URL}grid:servicerestart:status"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_servicerestart_status(data: dict) -> dict:
    """Create a new grid:servicerestart:status object."""
    url = f"{BASE_URL}grid:servicerestart:status"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_servicerestart_status(ref: str, data: dict) -> dict:
    """Update a grid:servicerestart:status object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_servicerestart_status(ref: str) -> dict:
    """Delete a grid:servicerestart:status object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_threatanalytics(**kwargs) -> dict:
    """Search for grid:threatanalytics objects."""
    url = f"{BASE_URL}grid:threatanalytics"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_threatanalytics(data: dict) -> dict:
    """Create a new grid:threatanalytics object."""
    url = f"{BASE_URL}grid:threatanalytics"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_threatanalytics(ref: str, data: dict) -> dict:
    """Update a grid:threatanalytics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_threatanalytics(ref: str) -> dict:
    """Delete a grid:threatanalytics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_threatprotection(**kwargs) -> dict:
    """Search for grid:threatprotection objects."""
    url = f"{BASE_URL}grid:threatprotection"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_threatprotection(data: dict) -> dict:
    """Create a new grid:threatprotection object."""
    url = f"{BASE_URL}grid:threatprotection"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_threatprotection(ref: str, data: dict) -> dict:
    """Update a grid:threatprotection object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_threatprotection(ref: str) -> dict:
    """Delete a grid:threatprotection object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_grid_x509certificate(**kwargs) -> dict:
    """Search for grid:x509certificate objects."""
    url = f"{BASE_URL}grid:x509certificate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_grid_x509certificate(data: dict) -> dict:
    """Create a new grid:x509certificate object."""
    url = f"{BASE_URL}grid:x509certificate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_grid_x509certificate(ref: str, data: dict) -> dict:
    """Update a grid:x509certificate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_grid_x509certificate(ref: str) -> dict:
    """Delete a grid:x509certificate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_hostnamerewritepolicy(**kwargs) -> dict:
    """Search for hostnamerewritepolicy objects."""
    url = f"{BASE_URL}hostnamerewritepolicy"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_hostnamerewritepolicy(data: dict) -> dict:
    """Create a new hostnamerewritepolicy object."""
    url = f"{BASE_URL}hostnamerewritepolicy"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_hostnamerewritepolicy(ref: str, data: dict) -> dict:
    """Update a hostnamerewritepolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_hostnamerewritepolicy(ref: str) -> dict:
    """Delete a hostnamerewritepolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_hsm_allgroups(**kwargs) -> dict:
    """Search for hsm:allgroups objects."""
    url = f"{BASE_URL}hsm:allgroups"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_hsm_allgroups(data: dict) -> dict:
    """Create a new hsm:allgroups object."""
    url = f"{BASE_URL}hsm:allgroups"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_hsm_allgroups(ref: str, data: dict) -> dict:
    """Update a hsm:allgroups object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_hsm_allgroups(ref: str) -> dict:
    """Delete a hsm:allgroups object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_hsm_entrustnshieldgroup(**kwargs) -> dict:
    """Search for hsm:entrustnshieldgroup objects."""
    url = f"{BASE_URL}hsm:entrustnshieldgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_hsm_entrustnshieldgroup(data: dict) -> dict:
    """Create a new hsm:entrustnshieldgroup object."""
    url = f"{BASE_URL}hsm:entrustnshieldgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_hsm_entrustnshieldgroup(ref: str, data: dict) -> dict:
    """Update a hsm:entrustnshieldgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_hsm_entrustnshieldgroup(ref: str) -> dict:
    """Delete a hsm:entrustnshieldgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_hsm_thaleslunagroup(**kwargs) -> dict:
    """Search for hsm:thaleslunagroup objects."""
    url = f"{BASE_URL}hsm:thaleslunagroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_hsm_thaleslunagroup(data: dict) -> dict:
    """Create a new hsm:thaleslunagroup object."""
    url = f"{BASE_URL}hsm:thaleslunagroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_hsm_thaleslunagroup(ref: str, data: dict) -> dict:
    """Update a hsm:thaleslunagroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_hsm_thaleslunagroup(ref: str) -> dict:
    """Delete a hsm:thaleslunagroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipam_statistics(**kwargs) -> dict:
    """Search for ipam:statistics objects."""
    url = f"{BASE_URL}ipam:statistics"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipam_statistics(data: dict) -> dict:
    """Create a new ipam:statistics object."""
    url = f"{BASE_URL}ipam:statistics"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipam_statistics(ref: str, data: dict) -> dict:
    """Update a ipam:statistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipam_statistics(ref: str) -> dict:
    """Delete a ipam:statistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv4address(**kwargs) -> dict:
    """Search for ipv4address objects."""
    url = f"{BASE_URL}ipv4address"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv4address(data: dict) -> dict:
    """Create a new ipv4address object."""
    url = f"{BASE_URL}ipv4address"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv4address(ref: str, data: dict) -> dict:
    """Update a ipv4address object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv4address(ref: str) -> dict:
    """Delete a ipv4address object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6address(**kwargs) -> dict:
    """Search for ipv6address objects."""
    url = f"{BASE_URL}ipv6address"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6address(data: dict) -> dict:
    """Create a new ipv6address object."""
    url = f"{BASE_URL}ipv6address"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6address(ref: str, data: dict) -> dict:
    """Update a ipv6address object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6address(ref: str) -> dict:
    """Delete a ipv6address object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6dhcpoptiondefinition(**kwargs) -> dict:
    """Search for ipv6dhcpoptiondefinition objects."""
    url = f"{BASE_URL}ipv6dhcpoptiondefinition"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6dhcpoptiondefinition(data: dict) -> dict:
    """Create a new ipv6dhcpoptiondefinition object."""
    url = f"{BASE_URL}ipv6dhcpoptiondefinition"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6dhcpoptiondefinition(ref: str, data: dict) -> dict:
    """Update a ipv6dhcpoptiondefinition object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6dhcpoptiondefinition(ref: str) -> dict:
    """Delete a ipv6dhcpoptiondefinition object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6dhcpoptionspace(**kwargs) -> dict:
    """Search for ipv6dhcpoptionspace objects."""
    url = f"{BASE_URL}ipv6dhcpoptionspace"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6dhcpoptionspace(data: dict) -> dict:
    """Create a new ipv6dhcpoptionspace object."""
    url = f"{BASE_URL}ipv6dhcpoptionspace"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6dhcpoptionspace(ref: str, data: dict) -> dict:
    """Update a ipv6dhcpoptionspace object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6dhcpoptionspace(ref: str) -> dict:
    """Delete a ipv6dhcpoptionspace object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6filteroption(**kwargs) -> dict:
    """Search for ipv6filteroption objects."""
    url = f"{BASE_URL}ipv6filteroption"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6filteroption(data: dict) -> dict:
    """Create a new ipv6filteroption object."""
    url = f"{BASE_URL}ipv6filteroption"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6filteroption(ref: str, data: dict) -> dict:
    """Update a ipv6filteroption object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6filteroption(ref: str) -> dict:
    """Delete a ipv6filteroption object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6fixedaddress(**kwargs) -> dict:
    """Search for ipv6fixedaddress objects."""
    url = f"{BASE_URL}ipv6fixedaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6fixedaddress(data: dict) -> dict:
    """Create a new ipv6fixedaddress object."""
    url = f"{BASE_URL}ipv6fixedaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6fixedaddress(ref: str, data: dict) -> dict:
    """Update a ipv6fixedaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6fixedaddress(ref: str) -> dict:
    """Delete a ipv6fixedaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6fixedaddresstemplate(**kwargs) -> dict:
    """Search for ipv6fixedaddresstemplate objects."""
    url = f"{BASE_URL}ipv6fixedaddresstemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6fixedaddresstemplate(data: dict) -> dict:
    """Create a new ipv6fixedaddresstemplate object."""
    url = f"{BASE_URL}ipv6fixedaddresstemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6fixedaddresstemplate(ref: str, data: dict) -> dict:
    """Update a ipv6fixedaddresstemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6fixedaddresstemplate(ref: str) -> dict:
    """Delete a ipv6fixedaddresstemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6network(**kwargs) -> dict:
    """Search for ipv6network objects."""
    url = f"{BASE_URL}ipv6network"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6network(data: dict) -> dict:
    """Create a new ipv6network object."""
    url = f"{BASE_URL}ipv6network"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6network(ref: str, data: dict) -> dict:
    """Update a ipv6network object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6network(ref: str) -> dict:
    """Delete a ipv6network object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6networkcontainer(**kwargs) -> dict:
    """Search for ipv6networkcontainer objects."""
    url = f"{BASE_URL}ipv6networkcontainer"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6networkcontainer(data: dict) -> dict:
    """Create a new ipv6networkcontainer object."""
    url = f"{BASE_URL}ipv6networkcontainer"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6networkcontainer(ref: str, data: dict) -> dict:
    """Update a ipv6networkcontainer object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6networkcontainer(ref: str) -> dict:
    """Delete a ipv6networkcontainer object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6networktemplate(**kwargs) -> dict:
    """Search for ipv6networktemplate objects."""
    url = f"{BASE_URL}ipv6networktemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6networktemplate(data: dict) -> dict:
    """Create a new ipv6networktemplate object."""
    url = f"{BASE_URL}ipv6networktemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6networktemplate(ref: str, data: dict) -> dict:
    """Update a ipv6networktemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6networktemplate(ref: str) -> dict:
    """Delete a ipv6networktemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6range(**kwargs) -> dict:
    """Search for ipv6range objects."""
    url = f"{BASE_URL}ipv6range"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6range(data: dict) -> dict:
    """Create a new ipv6range object."""
    url = f"{BASE_URL}ipv6range"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6range(ref: str, data: dict) -> dict:
    """Update a ipv6range object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6range(ref: str) -> dict:
    """Delete a ipv6range object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6rangetemplate(**kwargs) -> dict:
    """Search for ipv6rangetemplate objects."""
    url = f"{BASE_URL}ipv6rangetemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6rangetemplate(data: dict) -> dict:
    """Create a new ipv6rangetemplate object."""
    url = f"{BASE_URL}ipv6rangetemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6rangetemplate(ref: str, data: dict) -> dict:
    """Update a ipv6rangetemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6rangetemplate(ref: str) -> dict:
    """Delete a ipv6rangetemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ipv6sharednetwork(**kwargs) -> dict:
    """Search for ipv6sharednetwork objects."""
    url = f"{BASE_URL}ipv6sharednetwork"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ipv6sharednetwork(data: dict) -> dict:
    """Create a new ipv6sharednetwork object."""
    url = f"{BASE_URL}ipv6sharednetwork"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ipv6sharednetwork(ref: str, data: dict) -> dict:
    """Update a ipv6sharednetwork object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ipv6sharednetwork(ref: str) -> dict:
    """Delete a ipv6sharednetwork object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_kerberoskey(**kwargs) -> dict:
    """Search for kerberoskey objects."""
    url = f"{BASE_URL}kerberoskey"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_kerberoskey(data: dict) -> dict:
    """Create a new kerberoskey object."""
    url = f"{BASE_URL}kerberoskey"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_kerberoskey(ref: str, data: dict) -> dict:
    """Update a kerberoskey object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_kerberoskey(ref: str) -> dict:
    """Delete a kerberoskey object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ldap_auth_service(**kwargs) -> dict:
    """Search for ldap_auth_service objects."""
    url = f"{BASE_URL}ldap_auth_service"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ldap_auth_service(data: dict) -> dict:
    """Create a new ldap_auth_service object."""
    url = f"{BASE_URL}ldap_auth_service"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ldap_auth_service(ref: str, data: dict) -> dict:
    """Update a ldap_auth_service object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ldap_auth_service(ref: str) -> dict:
    """Delete a ldap_auth_service object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_lease(**kwargs) -> dict:
    """Search for lease objects."""
    url = f"{BASE_URL}lease"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_lease(data: dict) -> dict:
    """Create a new lease object."""
    url = f"{BASE_URL}lease"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_lease(ref: str, data: dict) -> dict:
    """Update a lease object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_lease(ref: str) -> dict:
    """Delete a lease object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_license_gridwide(**kwargs) -> dict:
    """Search for license:gridwide objects."""
    url = f"{BASE_URL}license:gridwide"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_license_gridwide(data: dict) -> dict:
    """Create a new license:gridwide object."""
    url = f"{BASE_URL}license:gridwide"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_license_gridwide(ref: str, data: dict) -> dict:
    """Update a license:gridwide object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_license_gridwide(ref: str) -> dict:
    """Delete a license:gridwide object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_localuser_authservice(**kwargs) -> dict:
    """Search for localuser:authservice objects."""
    url = f"{BASE_URL}localuser:authservice"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_localuser_authservice(data: dict) -> dict:
    """Create a new localuser:authservice object."""
    url = f"{BASE_URL}localuser:authservice"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_localuser_authservice(ref: str, data: dict) -> dict:
    """Update a localuser:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_localuser_authservice(ref: str) -> dict:
    """Delete a localuser:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_macfilteraddress(**kwargs) -> dict:
    """Search for macfilteraddress objects."""
    url = f"{BASE_URL}macfilteraddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_macfilteraddress(data: dict) -> dict:
    """Create a new macfilteraddress object."""
    url = f"{BASE_URL}macfilteraddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_macfilteraddress(ref: str, data: dict) -> dict:
    """Update a macfilteraddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_macfilteraddress(ref: str) -> dict:
    """Delete a macfilteraddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_mastergrid(**kwargs) -> dict:
    """Search for mastergrid objects."""
    url = f"{BASE_URL}mastergrid"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_mastergrid(data: dict) -> dict:
    """Create a new mastergrid object."""
    url = f"{BASE_URL}mastergrid"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_mastergrid(ref: str, data: dict) -> dict:
    """Update a mastergrid object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_mastergrid(ref: str) -> dict:
    """Delete a mastergrid object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member(**kwargs) -> dict:
    """Search for member objects."""
    url = f"{BASE_URL}member"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member(data: dict) -> dict:
    """Create a new member object."""
    url = f"{BASE_URL}member"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member(ref: str, data: dict) -> dict:
    """Update a member object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member(ref: str) -> dict:
    """Delete a member object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_dhcpproperties(**kwargs) -> dict:
    """Search for member:dhcpproperties objects."""
    url = f"{BASE_URL}member:dhcpproperties"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_dhcpproperties(data: dict) -> dict:
    """Create a new member:dhcpproperties object."""
    url = f"{BASE_URL}member:dhcpproperties"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_dhcpproperties(ref: str, data: dict) -> dict:
    """Update a member:dhcpproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_dhcpproperties(ref: str) -> dict:
    """Delete a member:dhcpproperties object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_dns(**kwargs) -> dict:
    """Search for member:dns objects."""
    url = f"{BASE_URL}member:dns"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_dns(data: dict) -> dict:
    """Create a new member:dns object."""
    url = f"{BASE_URL}member:dns"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_dns(ref: str, data: dict) -> dict:
    """Update a member:dns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_dns(ref: str) -> dict:
    """Delete a member:dns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_filedistribution(**kwargs) -> dict:
    """Search for member:filedistribution objects."""
    url = f"{BASE_URL}member:filedistribution"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_filedistribution(data: dict) -> dict:
    """Create a new member:filedistribution object."""
    url = f"{BASE_URL}member:filedistribution"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_filedistribution(ref: str, data: dict) -> dict:
    """Update a member:filedistribution object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_filedistribution(ref: str) -> dict:
    """Delete a member:filedistribution object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_license(**kwargs) -> dict:
    """Search for member:license objects."""
    url = f"{BASE_URL}member:license"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_license(data: dict) -> dict:
    """Create a new member:license object."""
    url = f"{BASE_URL}member:license"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_license(ref: str, data: dict) -> dict:
    """Update a member:license object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_license(ref: str) -> dict:
    """Delete a member:license object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_parentalcontrol(**kwargs) -> dict:
    """Search for member:parentalcontrol objects."""
    url = f"{BASE_URL}member:parentalcontrol"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_parentalcontrol(data: dict) -> dict:
    """Create a new member:parentalcontrol object."""
    url = f"{BASE_URL}member:parentalcontrol"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_parentalcontrol(ref: str, data: dict) -> dict:
    """Update a member:parentalcontrol object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_parentalcontrol(ref: str) -> dict:
    """Delete a member:parentalcontrol object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_threatanalytics(**kwargs) -> dict:
    """Search for member:threatanalytics objects."""
    url = f"{BASE_URL}member:threatanalytics"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_threatanalytics(data: dict) -> dict:
    """Create a new member:threatanalytics object."""
    url = f"{BASE_URL}member:threatanalytics"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_threatanalytics(ref: str, data: dict) -> dict:
    """Update a member:threatanalytics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_threatanalytics(ref: str) -> dict:
    """Delete a member:threatanalytics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_member_threatprotection(**kwargs) -> dict:
    """Search for member:threatprotection objects."""
    url = f"{BASE_URL}member:threatprotection"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_member_threatprotection(data: dict) -> dict:
    """Create a new member:threatprotection object."""
    url = f"{BASE_URL}member:threatprotection"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_member_threatprotection(ref: str, data: dict) -> dict:
    """Update a member:threatprotection object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_member_threatprotection(ref: str) -> dict:
    """Delete a member:threatprotection object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_memberclouddnssync(**kwargs) -> dict:
    """Search for memberclouddnssync objects."""
    url = f"{BASE_URL}memberclouddnssync"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_memberclouddnssync(data: dict) -> dict:
    """Create a new memberclouddnssync object."""
    url = f"{BASE_URL}memberclouddnssync"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_memberclouddnssync(ref: str, data: dict) -> dict:
    """Update a memberclouddnssync object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_memberclouddnssync(ref: str) -> dict:
    """Delete a memberclouddnssync object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_memberdfp(**kwargs) -> dict:
    """Search for memberdfp objects."""
    url = f"{BASE_URL}memberdfp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_memberdfp(data: dict) -> dict:
    """Create a new memberdfp object."""
    url = f"{BASE_URL}memberdfp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_memberdfp(ref: str, data: dict) -> dict:
    """Update a memberdfp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_memberdfp(ref: str) -> dict:
    """Delete a memberdfp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_msserver(**kwargs) -> dict:
    """Search for msserver objects."""
    url = f"{BASE_URL}msserver"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_msserver(data: dict) -> dict:
    """Create a new msserver object."""
    url = f"{BASE_URL}msserver"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_msserver(ref: str, data: dict) -> dict:
    """Update a msserver object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_msserver(ref: str) -> dict:
    """Delete a msserver object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_msserver_adsites_domain(**kwargs) -> dict:
    """Search for msserver:adsites:domain objects."""
    url = f"{BASE_URL}msserver:adsites:domain"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_msserver_adsites_domain(data: dict) -> dict:
    """Create a new msserver:adsites:domain object."""
    url = f"{BASE_URL}msserver:adsites:domain"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_msserver_adsites_domain(ref: str, data: dict) -> dict:
    """Update a msserver:adsites:domain object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_msserver_adsites_domain(ref: str) -> dict:
    """Delete a msserver:adsites:domain object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_msserver_adsites_site(**kwargs) -> dict:
    """Search for msserver:adsites:site objects."""
    url = f"{BASE_URL}msserver:adsites:site"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_msserver_adsites_site(data: dict) -> dict:
    """Create a new msserver:adsites:site object."""
    url = f"{BASE_URL}msserver:adsites:site"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_msserver_adsites_site(ref: str, data: dict) -> dict:
    """Update a msserver:adsites:site object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_msserver_adsites_site(ref: str) -> dict:
    """Delete a msserver:adsites:site object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_msserver_dhcp(**kwargs) -> dict:
    """Search for msserver:dhcp objects."""
    url = f"{BASE_URL}msserver:dhcp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_msserver_dhcp(data: dict) -> dict:
    """Create a new msserver:dhcp object."""
    url = f"{BASE_URL}msserver:dhcp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_msserver_dhcp(ref: str, data: dict) -> dict:
    """Update a msserver:dhcp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_msserver_dhcp(ref: str) -> dict:
    """Delete a msserver:dhcp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_msserver_dns(**kwargs) -> dict:
    """Search for msserver:dns objects."""
    url = f"{BASE_URL}msserver:dns"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_msserver_dns(data: dict) -> dict:
    """Create a new msserver:dns object."""
    url = f"{BASE_URL}msserver:dns"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_msserver_dns(ref: str, data: dict) -> dict:
    """Update a msserver:dns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_msserver_dns(ref: str) -> dict:
    """Delete a msserver:dns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_mssuperscope(**kwargs) -> dict:
    """Search for mssuperscope objects."""
    url = f"{BASE_URL}mssuperscope"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_mssuperscope(data: dict) -> dict:
    """Create a new mssuperscope object."""
    url = f"{BASE_URL}mssuperscope"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_mssuperscope(ref: str, data: dict) -> dict:
    """Update a mssuperscope object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_mssuperscope(ref: str) -> dict:
    """Delete a mssuperscope object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_namedacl(**kwargs) -> dict:
    """Search for namedacl objects."""
    url = f"{BASE_URL}namedacl"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_namedacl(data: dict) -> dict:
    """Create a new namedacl object."""
    url = f"{BASE_URL}namedacl"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_namedacl(ref: str, data: dict) -> dict:
    """Update a namedacl object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_namedacl(ref: str) -> dict:
    """Delete a namedacl object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_natgroup(**kwargs) -> dict:
    """Search for natgroup objects."""
    url = f"{BASE_URL}natgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_natgroup(data: dict) -> dict:
    """Create a new natgroup object."""
    url = f"{BASE_URL}natgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_natgroup(ref: str, data: dict) -> dict:
    """Update a natgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_natgroup(ref: str) -> dict:
    """Delete a natgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_network(**kwargs) -> dict:
    """Search for network objects."""
    url = f"{BASE_URL}network"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_network(data: dict) -> dict:
    """Create a new network object."""
    url = f"{BASE_URL}network"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_network(ref: str, data: dict) -> dict:
    """Update a network object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_network(ref: str) -> dict:
    """Delete a network object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_network_discovery(**kwargs) -> dict:
    """Search for network_discovery objects."""
    url = f"{BASE_URL}network_discovery"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_network_discovery(data: dict) -> dict:
    """Create a new network_discovery object."""
    url = f"{BASE_URL}network_discovery"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_network_discovery(ref: str, data: dict) -> dict:
    """Update a network_discovery object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_network_discovery(ref: str) -> dict:
    """Delete a network_discovery object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_networkcontainer(**kwargs) -> dict:
    """Search for networkcontainer objects."""
    url = f"{BASE_URL}networkcontainer"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_networkcontainer(data: dict) -> dict:
    """Create a new networkcontainer object."""
    url = f"{BASE_URL}networkcontainer"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_networkcontainer(ref: str, data: dict) -> dict:
    """Update a networkcontainer object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_networkcontainer(ref: str) -> dict:
    """Delete a networkcontainer object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_networktemplate(**kwargs) -> dict:
    """Search for networktemplate objects."""
    url = f"{BASE_URL}networktemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_networktemplate(data: dict) -> dict:
    """Create a new networktemplate object."""
    url = f"{BASE_URL}networktemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_networktemplate(ref: str, data: dict) -> dict:
    """Update a networktemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_networktemplate(ref: str) -> dict:
    """Delete a networktemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_networkuser(**kwargs) -> dict:
    """Search for networkuser objects."""
    url = f"{BASE_URL}networkuser"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_networkuser(data: dict) -> dict:
    """Create a new networkuser object."""
    url = f"{BASE_URL}networkuser"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_networkuser(ref: str, data: dict) -> dict:
    """Update a networkuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_networkuser(ref: str) -> dict:
    """Delete a networkuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_networkview(**kwargs) -> dict:
    """Search for networkview objects."""
    url = f"{BASE_URL}networkview"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_networkview(data: dict) -> dict:
    """Create a new networkview object."""
    url = f"{BASE_URL}networkview"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_networkview(ref: str, data: dict) -> dict:
    """Update a networkview object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_networkview(ref: str) -> dict:
    """Delete a networkview object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_notification_rest_endpoint(**kwargs) -> dict:
    """Search for notification:rest:endpoint objects."""
    url = f"{BASE_URL}notification:rest:endpoint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_notification_rest_endpoint(data: dict) -> dict:
    """Create a new notification:rest:endpoint object."""
    url = f"{BASE_URL}notification:rest:endpoint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_notification_rest_endpoint(ref: str, data: dict) -> dict:
    """Update a notification:rest:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_notification_rest_endpoint(ref: str) -> dict:
    """Delete a notification:rest:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_notification_rest_template(**kwargs) -> dict:
    """Search for notification:rest:template objects."""
    url = f"{BASE_URL}notification:rest:template"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_notification_rest_template(data: dict) -> dict:
    """Create a new notification:rest:template object."""
    url = f"{BASE_URL}notification:rest:template"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_notification_rest_template(ref: str, data: dict) -> dict:
    """Update a notification:rest:template object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_notification_rest_template(ref: str) -> dict:
    """Delete a notification:rest:template object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_notification_rule(**kwargs) -> dict:
    """Search for notification:rule objects."""
    url = f"{BASE_URL}notification:rule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_notification_rule(data: dict) -> dict:
    """Create a new notification:rule object."""
    url = f"{BASE_URL}notification:rule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_notification_rule(ref: str, data: dict) -> dict:
    """Update a notification:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_notification_rule(ref: str) -> dict:
    """Delete a notification:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_nsgroup(**kwargs) -> dict:
    """Search for nsgroup objects."""
    url = f"{BASE_URL}nsgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_nsgroup(data: dict) -> dict:
    """Create a new nsgroup object."""
    url = f"{BASE_URL}nsgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_nsgroup(ref: str, data: dict) -> dict:
    """Update a nsgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_nsgroup(ref: str) -> dict:
    """Delete a nsgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_nsgroup_delegation(**kwargs) -> dict:
    """Search for nsgroup:delegation objects."""
    url = f"{BASE_URL}nsgroup:delegation"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_nsgroup_delegation(data: dict) -> dict:
    """Create a new nsgroup:delegation object."""
    url = f"{BASE_URL}nsgroup:delegation"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_nsgroup_delegation(ref: str, data: dict) -> dict:
    """Update a nsgroup:delegation object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_nsgroup_delegation(ref: str) -> dict:
    """Delete a nsgroup:delegation object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_nsgroup_forwardingmember(**kwargs) -> dict:
    """Search for nsgroup:forwardingmember objects."""
    url = f"{BASE_URL}nsgroup:forwardingmember"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_nsgroup_forwardingmember(data: dict) -> dict:
    """Create a new nsgroup:forwardingmember object."""
    url = f"{BASE_URL}nsgroup:forwardingmember"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_nsgroup_forwardingmember(ref: str, data: dict) -> dict:
    """Update a nsgroup:forwardingmember object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_nsgroup_forwardingmember(ref: str) -> dict:
    """Delete a nsgroup:forwardingmember object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_nsgroup_forwardstubserver(**kwargs) -> dict:
    """Search for nsgroup:forwardstubserver objects."""
    url = f"{BASE_URL}nsgroup:forwardstubserver"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_nsgroup_forwardstubserver(data: dict) -> dict:
    """Create a new nsgroup:forwardstubserver object."""
    url = f"{BASE_URL}nsgroup:forwardstubserver"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_nsgroup_forwardstubserver(ref: str, data: dict) -> dict:
    """Update a nsgroup:forwardstubserver object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_nsgroup_forwardstubserver(ref: str) -> dict:
    """Delete a nsgroup:forwardstubserver object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_nsgroup_stubmember(**kwargs) -> dict:
    """Search for nsgroup:stubmember objects."""
    url = f"{BASE_URL}nsgroup:stubmember"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_nsgroup_stubmember(data: dict) -> dict:
    """Create a new nsgroup:stubmember object."""
    url = f"{BASE_URL}nsgroup:stubmember"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_nsgroup_stubmember(ref: str, data: dict) -> dict:
    """Update a nsgroup:stubmember object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_nsgroup_stubmember(ref: str) -> dict:
    """Delete a nsgroup:stubmember object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_orderedranges(**kwargs) -> dict:
    """Search for orderedranges objects."""
    url = f"{BASE_URL}orderedranges"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_orderedranges(data: dict) -> dict:
    """Create a new orderedranges object."""
    url = f"{BASE_URL}orderedranges"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_orderedranges(ref: str, data: dict) -> dict:
    """Update a orderedranges object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_orderedranges(ref: str) -> dict:
    """Delete a orderedranges object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_orderedresponsepolicyzones(**kwargs) -> dict:
    """Search for orderedresponsepolicyzones objects."""
    url = f"{BASE_URL}orderedresponsepolicyzones"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_orderedresponsepolicyzones(data: dict) -> dict:
    """Create a new orderedresponsepolicyzones object."""
    url = f"{BASE_URL}orderedresponsepolicyzones"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_orderedresponsepolicyzones(ref: str, data: dict) -> dict:
    """Update a orderedresponsepolicyzones object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_orderedresponsepolicyzones(ref: str) -> dict:
    """Delete a orderedresponsepolicyzones object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_outbound_cloudclient(**kwargs) -> dict:
    """Search for outbound:cloudclient objects."""
    url = f"{BASE_URL}outbound:cloudclient"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_outbound_cloudclient(data: dict) -> dict:
    """Create a new outbound:cloudclient object."""
    url = f"{BASE_URL}outbound:cloudclient"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_outbound_cloudclient(ref: str, data: dict) -> dict:
    """Update a outbound:cloudclient object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_outbound_cloudclient(ref: str) -> dict:
    """Delete a outbound:cloudclient object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_parentalcontrol_avp(**kwargs) -> dict:
    """Search for parentalcontrol:avp objects."""
    url = f"{BASE_URL}parentalcontrol:avp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_parentalcontrol_avp(data: dict) -> dict:
    """Create a new parentalcontrol:avp object."""
    url = f"{BASE_URL}parentalcontrol:avp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_parentalcontrol_avp(ref: str, data: dict) -> dict:
    """Update a parentalcontrol:avp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_parentalcontrol_avp(ref: str) -> dict:
    """Delete a parentalcontrol:avp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_parentalcontrol_blockingpolicy(**kwargs) -> dict:
    """Search for parentalcontrol:blockingpolicy objects."""
    url = f"{BASE_URL}parentalcontrol:blockingpolicy"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_parentalcontrol_blockingpolicy(data: dict) -> dict:
    """Create a new parentalcontrol:blockingpolicy object."""
    url = f"{BASE_URL}parentalcontrol:blockingpolicy"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_parentalcontrol_blockingpolicy(ref: str, data: dict) -> dict:
    """Update a parentalcontrol:blockingpolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_parentalcontrol_blockingpolicy(ref: str) -> dict:
    """Delete a parentalcontrol:blockingpolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_parentalcontrol_subscriber(**kwargs) -> dict:
    """Search for parentalcontrol:subscriber objects."""
    url = f"{BASE_URL}parentalcontrol:subscriber"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_parentalcontrol_subscriber(data: dict) -> dict:
    """Create a new parentalcontrol:subscriber object."""
    url = f"{BASE_URL}parentalcontrol:subscriber"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_parentalcontrol_subscriber(ref: str, data: dict) -> dict:
    """Update a parentalcontrol:subscriber object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_parentalcontrol_subscriber(ref: str) -> dict:
    """Delete a parentalcontrol:subscriber object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_parentalcontrol_subscriberrecord(**kwargs) -> dict:
    """Search for parentalcontrol:subscriberrecord objects."""
    url = f"{BASE_URL}parentalcontrol:subscriberrecord"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_parentalcontrol_subscriberrecord(data: dict) -> dict:
    """Create a new parentalcontrol:subscriberrecord object."""
    url = f"{BASE_URL}parentalcontrol:subscriberrecord"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_parentalcontrol_subscriberrecord(ref: str, data: dict) -> dict:
    """Update a parentalcontrol:subscriberrecord object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_parentalcontrol_subscriberrecord(ref: str) -> dict:
    """Delete a parentalcontrol:subscriberrecord object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_parentalcontrol_subscribersite(**kwargs) -> dict:
    """Search for parentalcontrol:subscribersite objects."""
    url = f"{BASE_URL}parentalcontrol:subscribersite"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_parentalcontrol_subscribersite(data: dict) -> dict:
    """Create a new parentalcontrol:subscribersite object."""
    url = f"{BASE_URL}parentalcontrol:subscribersite"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_parentalcontrol_subscribersite(ref: str, data: dict) -> dict:
    """Update a parentalcontrol:subscribersite object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_parentalcontrol_subscribersite(ref: str) -> dict:
    """Delete a parentalcontrol:subscribersite object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_permission(**kwargs) -> dict:
    """Search for permission objects."""
    url = f"{BASE_URL}permission"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_permission(data: dict) -> dict:
    """Create a new permission object."""
    url = f"{BASE_URL}permission"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_permission(ref: str, data: dict) -> dict:
    """Update a permission object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_permission(ref: str) -> dict:
    """Delete a permission object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_pxgrid_endpoint(**kwargs) -> dict:
    """Search for pxgrid:endpoint objects."""
    url = f"{BASE_URL}pxgrid:endpoint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_pxgrid_endpoint(data: dict) -> dict:
    """Create a new pxgrid:endpoint object."""
    url = f"{BASE_URL}pxgrid:endpoint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_pxgrid_endpoint(ref: str, data: dict) -> dict:
    """Update a pxgrid:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_pxgrid_endpoint(ref: str) -> dict:
    """Delete a pxgrid:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_radius_authservice(**kwargs) -> dict:
    """Search for radius:authservice objects."""
    url = f"{BASE_URL}radius:authservice"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_radius_authservice(data: dict) -> dict:
    """Create a new radius:authservice object."""
    url = f"{BASE_URL}radius:authservice"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_radius_authservice(ref: str, data: dict) -> dict:
    """Update a radius:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_radius_authservice(ref: str) -> dict:
    """Delete a radius:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_range(**kwargs) -> dict:
    """Search for range objects."""
    url = f"{BASE_URL}range"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_range(data: dict) -> dict:
    """Create a new range object."""
    url = f"{BASE_URL}range"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_range(ref: str, data: dict) -> dict:
    """Update a range object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_range(ref: str) -> dict:
    """Delete a range object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_rangetemplate(**kwargs) -> dict:
    """Search for rangetemplate objects."""
    url = f"{BASE_URL}rangetemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_rangetemplate(data: dict) -> dict:
    """Create a new rangetemplate object."""
    url = f"{BASE_URL}rangetemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_rangetemplate(ref: str, data: dict) -> dict:
    """Update a rangetemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_rangetemplate(ref: str) -> dict:
    """Delete a rangetemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_a(**kwargs) -> dict:
    """Search for record:a objects."""
    url = f"{BASE_URL}record:a"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_a(data: dict) -> dict:
    """Create a new record:a object."""
    url = f"{BASE_URL}record:a"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_a(ref: str, data: dict) -> dict:
    """Update a record:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_a(ref: str) -> dict:
    """Delete a record:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_aaaa(**kwargs) -> dict:
    """Search for record:aaaa objects."""
    url = f"{BASE_URL}record:aaaa"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_aaaa(data: dict) -> dict:
    """Create a new record:aaaa object."""
    url = f"{BASE_URL}record:aaaa"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_aaaa(ref: str, data: dict) -> dict:
    """Update a record:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_aaaa(ref: str) -> dict:
    """Delete a record:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_alias(**kwargs) -> dict:
    """Search for record:alias objects."""
    url = f"{BASE_URL}record:alias"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_alias(data: dict) -> dict:
    """Create a new record:alias object."""
    url = f"{BASE_URL}record:alias"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_alias(ref: str, data: dict) -> dict:
    """Update a record:alias object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_alias(ref: str) -> dict:
    """Delete a record:alias object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_caa(**kwargs) -> dict:
    """Search for record:caa objects."""
    url = f"{BASE_URL}record:caa"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_caa(data: dict) -> dict:
    """Create a new record:caa object."""
    url = f"{BASE_URL}record:caa"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_caa(ref: str, data: dict) -> dict:
    """Update a record:caa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_caa(ref: str) -> dict:
    """Delete a record:caa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_cname(**kwargs) -> dict:
    """Search for record:cname objects."""
    url = f"{BASE_URL}record:cname"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_cname(data: dict) -> dict:
    """Create a new record:cname object."""
    url = f"{BASE_URL}record:cname"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_cname(ref: str, data: dict) -> dict:
    """Update a record:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_cname(ref: str) -> dict:
    """Delete a record:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_dhcid(**kwargs) -> dict:
    """Search for record:dhcid objects."""
    url = f"{BASE_URL}record:dhcid"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_dhcid(data: dict) -> dict:
    """Create a new record:dhcid object."""
    url = f"{BASE_URL}record:dhcid"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_dhcid(ref: str, data: dict) -> dict:
    """Update a record:dhcid object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_dhcid(ref: str) -> dict:
    """Delete a record:dhcid object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_dname(**kwargs) -> dict:
    """Search for record:dname objects."""
    url = f"{BASE_URL}record:dname"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_dname(data: dict) -> dict:
    """Create a new record:dname object."""
    url = f"{BASE_URL}record:dname"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_dname(ref: str, data: dict) -> dict:
    """Update a record:dname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_dname(ref: str) -> dict:
    """Delete a record:dname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_dnskey(**kwargs) -> dict:
    """Search for record:dnskey objects."""
    url = f"{BASE_URL}record:dnskey"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_dnskey(data: dict) -> dict:
    """Create a new record:dnskey object."""
    url = f"{BASE_URL}record:dnskey"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_dnskey(ref: str, data: dict) -> dict:
    """Update a record:dnskey object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_dnskey(ref: str) -> dict:
    """Delete a record:dnskey object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_ds(**kwargs) -> dict:
    """Search for record:ds objects."""
    url = f"{BASE_URL}record:ds"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_ds(data: dict) -> dict:
    """Create a new record:ds object."""
    url = f"{BASE_URL}record:ds"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_ds(ref: str, data: dict) -> dict:
    """Update a record:ds object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_ds(ref: str) -> dict:
    """Delete a record:ds object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_dtclbdn(**kwargs) -> dict:
    """Search for record:dtclbdn objects."""
    url = f"{BASE_URL}record:dtclbdn"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_dtclbdn(data: dict) -> dict:
    """Create a new record:dtclbdn object."""
    url = f"{BASE_URL}record:dtclbdn"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_dtclbdn(ref: str, data: dict) -> dict:
    """Update a record:dtclbdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_dtclbdn(ref: str) -> dict:
    """Delete a record:dtclbdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_host(**kwargs) -> dict:
    """Search for record:host objects."""
    url = f"{BASE_URL}record:host"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_host(data: dict) -> dict:
    """Create a new record:host object."""
    url = f"{BASE_URL}record:host"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_host(ref: str, data: dict) -> dict:
    """Update a record:host object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_host(ref: str) -> dict:
    """Delete a record:host object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_host_ipv4addr(**kwargs) -> dict:
    """Search for record:host_ipv4addr objects."""
    url = f"{BASE_URL}record:host_ipv4addr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_host_ipv4addr(data: dict) -> dict:
    """Create a new record:host_ipv4addr object."""
    url = f"{BASE_URL}record:host_ipv4addr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_host_ipv4addr(ref: str, data: dict) -> dict:
    """Update a record:host_ipv4addr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_host_ipv4addr(ref: str) -> dict:
    """Delete a record:host_ipv4addr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_host_ipv6addr(**kwargs) -> dict:
    """Search for record:host_ipv6addr objects."""
    url = f"{BASE_URL}record:host_ipv6addr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_host_ipv6addr(data: dict) -> dict:
    """Create a new record:host_ipv6addr object."""
    url = f"{BASE_URL}record:host_ipv6addr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_host_ipv6addr(ref: str, data: dict) -> dict:
    """Update a record:host_ipv6addr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_host_ipv6addr(ref: str) -> dict:
    """Delete a record:host_ipv6addr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_mx(**kwargs) -> dict:
    """Search for record:mx objects."""
    url = f"{BASE_URL}record:mx"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_mx(data: dict) -> dict:
    """Create a new record:mx object."""
    url = f"{BASE_URL}record:mx"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_mx(ref: str, data: dict) -> dict:
    """Update a record:mx object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_mx(ref: str) -> dict:
    """Delete a record:mx object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_naptr(**kwargs) -> dict:
    """Search for record:naptr objects."""
    url = f"{BASE_URL}record:naptr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_naptr(data: dict) -> dict:
    """Create a new record:naptr object."""
    url = f"{BASE_URL}record:naptr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_naptr(ref: str, data: dict) -> dict:
    """Update a record:naptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_naptr(ref: str) -> dict:
    """Delete a record:naptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_ns(**kwargs) -> dict:
    """Search for record:ns objects."""
    url = f"{BASE_URL}record:ns"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_ns(data: dict) -> dict:
    """Create a new record:ns object."""
    url = f"{BASE_URL}record:ns"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_ns(ref: str, data: dict) -> dict:
    """Update a record:ns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_ns(ref: str) -> dict:
    """Delete a record:ns object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_nsec(**kwargs) -> dict:
    """Search for record:nsec objects."""
    url = f"{BASE_URL}record:nsec"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_nsec(data: dict) -> dict:
    """Create a new record:nsec object."""
    url = f"{BASE_URL}record:nsec"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_nsec(ref: str, data: dict) -> dict:
    """Update a record:nsec object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_nsec(ref: str) -> dict:
    """Delete a record:nsec object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_nsec3(**kwargs) -> dict:
    """Search for record:nsec3 objects."""
    url = f"{BASE_URL}record:nsec3"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_nsec3(data: dict) -> dict:
    """Create a new record:nsec3 object."""
    url = f"{BASE_URL}record:nsec3"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_nsec3(ref: str, data: dict) -> dict:
    """Update a record:nsec3 object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_nsec3(ref: str) -> dict:
    """Delete a record:nsec3 object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_nsec3param(**kwargs) -> dict:
    """Search for record:nsec3param objects."""
    url = f"{BASE_URL}record:nsec3param"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_nsec3param(data: dict) -> dict:
    """Create a new record:nsec3param object."""
    url = f"{BASE_URL}record:nsec3param"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_nsec3param(ref: str, data: dict) -> dict:
    """Update a record:nsec3param object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_nsec3param(ref: str) -> dict:
    """Delete a record:nsec3param object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_ptr(**kwargs) -> dict:
    """Search for record:ptr objects."""
    url = f"{BASE_URL}record:ptr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_ptr(data: dict) -> dict:
    """Create a new record:ptr object."""
    url = f"{BASE_URL}record:ptr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_ptr(ref: str, data: dict) -> dict:
    """Update a record:ptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_ptr(ref: str) -> dict:
    """Delete a record:ptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_a(**kwargs) -> dict:
    """Search for record:rpz:a objects."""
    url = f"{BASE_URL}record:rpz:a"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_a(data: dict) -> dict:
    """Create a new record:rpz:a object."""
    url = f"{BASE_URL}record:rpz:a"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_a(ref: str, data: dict) -> dict:
    """Update a record:rpz:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_a(ref: str) -> dict:
    """Delete a record:rpz:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_a_ipaddress(**kwargs) -> dict:
    """Search for record:rpz:a:ipaddress objects."""
    url = f"{BASE_URL}record:rpz:a:ipaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_a_ipaddress(data: dict) -> dict:
    """Create a new record:rpz:a:ipaddress object."""
    url = f"{BASE_URL}record:rpz:a:ipaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_a_ipaddress(ref: str, data: dict) -> dict:
    """Update a record:rpz:a:ipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_a_ipaddress(ref: str) -> dict:
    """Delete a record:rpz:a:ipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_aaaa(**kwargs) -> dict:
    """Search for record:rpz:aaaa objects."""
    url = f"{BASE_URL}record:rpz:aaaa"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_aaaa(data: dict) -> dict:
    """Create a new record:rpz:aaaa object."""
    url = f"{BASE_URL}record:rpz:aaaa"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_aaaa(ref: str, data: dict) -> dict:
    """Update a record:rpz:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_aaaa(ref: str) -> dict:
    """Delete a record:rpz:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_aaaa_ipaddress(**kwargs) -> dict:
    """Search for record:rpz:aaaa:ipaddress objects."""
    url = f"{BASE_URL}record:rpz:aaaa:ipaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_aaaa_ipaddress(data: dict) -> dict:
    """Create a new record:rpz:aaaa:ipaddress object."""
    url = f"{BASE_URL}record:rpz:aaaa:ipaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_aaaa_ipaddress(ref: str, data: dict) -> dict:
    """Update a record:rpz:aaaa:ipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_aaaa_ipaddress(ref: str) -> dict:
    """Delete a record:rpz:aaaa:ipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_cname(**kwargs) -> dict:
    """Search for record:rpz:cname objects."""
    url = f"{BASE_URL}record:rpz:cname"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_cname(data: dict) -> dict:
    """Create a new record:rpz:cname object."""
    url = f"{BASE_URL}record:rpz:cname"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_cname(ref: str, data: dict) -> dict:
    """Update a record:rpz:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_cname(ref: str) -> dict:
    """Delete a record:rpz:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_cname_clientipaddress(**kwargs) -> dict:
    """Search for record:rpz:cname:clientipaddress objects."""
    url = f"{BASE_URL}record:rpz:cname:clientipaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_cname_clientipaddress(data: dict) -> dict:
    """Create a new record:rpz:cname:clientipaddress object."""
    url = f"{BASE_URL}record:rpz:cname:clientipaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_cname_clientipaddress(ref: str, data: dict) -> dict:
    """Update a record:rpz:cname:clientipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_cname_clientipaddress(ref: str) -> dict:
    """Delete a record:rpz:cname:clientipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_cname_clientipaddressdn(**kwargs) -> dict:
    """Search for record:rpz:cname:clientipaddressdn objects."""
    url = f"{BASE_URL}record:rpz:cname:clientipaddressdn"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_cname_clientipaddressdn(data: dict) -> dict:
    """Create a new record:rpz:cname:clientipaddressdn object."""
    url = f"{BASE_URL}record:rpz:cname:clientipaddressdn"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_cname_clientipaddressdn(ref: str, data: dict) -> dict:
    """Update a record:rpz:cname:clientipaddressdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_cname_clientipaddressdn(ref: str) -> dict:
    """Delete a record:rpz:cname:clientipaddressdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_cname_ipaddress(**kwargs) -> dict:
    """Search for record:rpz:cname:ipaddress objects."""
    url = f"{BASE_URL}record:rpz:cname:ipaddress"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_cname_ipaddress(data: dict) -> dict:
    """Create a new record:rpz:cname:ipaddress object."""
    url = f"{BASE_URL}record:rpz:cname:ipaddress"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_cname_ipaddress(ref: str, data: dict) -> dict:
    """Update a record:rpz:cname:ipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_cname_ipaddress(ref: str) -> dict:
    """Delete a record:rpz:cname:ipaddress object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_cname_ipaddressdn(**kwargs) -> dict:
    """Search for record:rpz:cname:ipaddressdn objects."""
    url = f"{BASE_URL}record:rpz:cname:ipaddressdn"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_cname_ipaddressdn(data: dict) -> dict:
    """Create a new record:rpz:cname:ipaddressdn object."""
    url = f"{BASE_URL}record:rpz:cname:ipaddressdn"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_cname_ipaddressdn(ref: str, data: dict) -> dict:
    """Update a record:rpz:cname:ipaddressdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_cname_ipaddressdn(ref: str) -> dict:
    """Delete a record:rpz:cname:ipaddressdn object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_mx(**kwargs) -> dict:
    """Search for record:rpz:mx objects."""
    url = f"{BASE_URL}record:rpz:mx"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_mx(data: dict) -> dict:
    """Create a new record:rpz:mx object."""
    url = f"{BASE_URL}record:rpz:mx"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_mx(ref: str, data: dict) -> dict:
    """Update a record:rpz:mx object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_mx(ref: str) -> dict:
    """Delete a record:rpz:mx object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_naptr(**kwargs) -> dict:
    """Search for record:rpz:naptr objects."""
    url = f"{BASE_URL}record:rpz:naptr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_naptr(data: dict) -> dict:
    """Create a new record:rpz:naptr object."""
    url = f"{BASE_URL}record:rpz:naptr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_naptr(ref: str, data: dict) -> dict:
    """Update a record:rpz:naptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_naptr(ref: str) -> dict:
    """Delete a record:rpz:naptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_ptr(**kwargs) -> dict:
    """Search for record:rpz:ptr objects."""
    url = f"{BASE_URL}record:rpz:ptr"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_ptr(data: dict) -> dict:
    """Create a new record:rpz:ptr object."""
    url = f"{BASE_URL}record:rpz:ptr"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_ptr(ref: str, data: dict) -> dict:
    """Update a record:rpz:ptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_ptr(ref: str) -> dict:
    """Delete a record:rpz:ptr object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_srv(**kwargs) -> dict:
    """Search for record:rpz:srv objects."""
    url = f"{BASE_URL}record:rpz:srv"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_srv(data: dict) -> dict:
    """Create a new record:rpz:srv object."""
    url = f"{BASE_URL}record:rpz:srv"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_srv(ref: str, data: dict) -> dict:
    """Update a record:rpz:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_srv(ref: str) -> dict:
    """Delete a record:rpz:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rpz_txt(**kwargs) -> dict:
    """Search for record:rpz:txt objects."""
    url = f"{BASE_URL}record:rpz:txt"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rpz_txt(data: dict) -> dict:
    """Create a new record:rpz:txt object."""
    url = f"{BASE_URL}record:rpz:txt"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rpz_txt(ref: str, data: dict) -> dict:
    """Update a record:rpz:txt object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rpz_txt(ref: str) -> dict:
    """Delete a record:rpz:txt object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_rrsig(**kwargs) -> dict:
    """Search for record:rrsig objects."""
    url = f"{BASE_URL}record:rrsig"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_rrsig(data: dict) -> dict:
    """Create a new record:rrsig object."""
    url = f"{BASE_URL}record:rrsig"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_rrsig(ref: str, data: dict) -> dict:
    """Update a record:rrsig object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_rrsig(ref: str) -> dict:
    """Delete a record:rrsig object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_srv(**kwargs) -> dict:
    """Search for record:srv objects."""
    url = f"{BASE_URL}record:srv"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_srv(data: dict) -> dict:
    """Create a new record:srv object."""
    url = f"{BASE_URL}record:srv"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_srv(ref: str, data: dict) -> dict:
    """Update a record:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_srv(ref: str) -> dict:
    """Delete a record:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_tlsa(**kwargs) -> dict:
    """Search for record:tlsa objects."""
    url = f"{BASE_URL}record:tlsa"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_tlsa(data: dict) -> dict:
    """Create a new record:tlsa object."""
    url = f"{BASE_URL}record:tlsa"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_tlsa(ref: str, data: dict) -> dict:
    """Update a record:tlsa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_tlsa(ref: str) -> dict:
    """Delete a record:tlsa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_txt(**kwargs) -> dict:
    """Search for record:txt objects."""
    url = f"{BASE_URL}record:txt"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_txt(data: dict) -> dict:
    """Create a new record:txt object."""
    url = f"{BASE_URL}record:txt"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_txt(ref: str, data: dict) -> dict:
    """Update a record:txt object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_txt(ref: str) -> dict:
    """Delete a record:txt object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_record_unknown(**kwargs) -> dict:
    """Search for record:unknown objects."""
    url = f"{BASE_URL}record:unknown"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_record_unknown(data: dict) -> dict:
    """Create a new record:unknown object."""
    url = f"{BASE_URL}record:unknown"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_record_unknown(ref: str, data: dict) -> dict:
    """Update a record:unknown object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_record_unknown(ref: str) -> dict:
    """Delete a record:unknown object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_recordnamepolicy(**kwargs) -> dict:
    """Search for recordnamepolicy objects."""
    url = f"{BASE_URL}recordnamepolicy"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_recordnamepolicy(data: dict) -> dict:
    """Create a new recordnamepolicy object."""
    url = f"{BASE_URL}recordnamepolicy"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_recordnamepolicy(ref: str, data: dict) -> dict:
    """Update a recordnamepolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_recordnamepolicy(ref: str) -> dict:
    """Delete a recordnamepolicy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_request(**kwargs) -> dict:
    """Search for request objects."""
    url = f"{BASE_URL}request"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_request(data: dict) -> dict:
    """Create a new request object."""
    url = f"{BASE_URL}request"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_request(ref: str, data: dict) -> dict:
    """Update a request object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_request(ref: str) -> dict:
    """Delete a request object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_restartservicestatus(**kwargs) -> dict:
    """Search for restartservicestatus objects."""
    url = f"{BASE_URL}restartservicestatus"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_restartservicestatus(data: dict) -> dict:
    """Create a new restartservicestatus object."""
    url = f"{BASE_URL}restartservicestatus"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_restartservicestatus(ref: str, data: dict) -> dict:
    """Update a restartservicestatus object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_restartservicestatus(ref: str) -> dict:
    """Delete a restartservicestatus object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_rir(**kwargs) -> dict:
    """Search for rir objects."""
    url = f"{BASE_URL}rir"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_rir(data: dict) -> dict:
    """Create a new rir object."""
    url = f"{BASE_URL}rir"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_rir(ref: str, data: dict) -> dict:
    """Update a rir object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_rir(ref: str) -> dict:
    """Delete a rir object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_rir_organization(**kwargs) -> dict:
    """Search for rir:organization objects."""
    url = f"{BASE_URL}rir:organization"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_rir_organization(data: dict) -> dict:
    """Create a new rir:organization object."""
    url = f"{BASE_URL}rir:organization"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_rir_organization(ref: str, data: dict) -> dict:
    """Update a rir:organization object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_rir_organization(ref: str) -> dict:
    """Delete a rir:organization object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_roaminghost(**kwargs) -> dict:
    """Search for roaminghost objects."""
    url = f"{BASE_URL}roaminghost"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_roaminghost(data: dict) -> dict:
    """Create a new roaminghost object."""
    url = f"{BASE_URL}roaminghost"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_roaminghost(ref: str, data: dict) -> dict:
    """Update a roaminghost object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_roaminghost(ref: str) -> dict:
    """Delete a roaminghost object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_ruleset(**kwargs) -> dict:
    """Search for ruleset objects."""
    url = f"{BASE_URL}ruleset"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_ruleset(data: dict) -> dict:
    """Create a new ruleset object."""
    url = f"{BASE_URL}ruleset"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_ruleset(ref: str, data: dict) -> dict:
    """Update a ruleset object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_ruleset(ref: str) -> dict:
    """Delete a ruleset object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_saml_authservice(**kwargs) -> dict:
    """Search for saml:authservice objects."""
    url = f"{BASE_URL}saml:authservice"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_saml_authservice(data: dict) -> dict:
    """Create a new saml:authservice object."""
    url = f"{BASE_URL}saml:authservice"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_saml_authservice(ref: str, data: dict) -> dict:
    """Update a saml:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_saml_authservice(ref: str) -> dict:
    """Delete a saml:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_scavengingtask(**kwargs) -> dict:
    """Search for scavengingtask objects."""
    url = f"{BASE_URL}scavengingtask"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_scavengingtask(data: dict) -> dict:
    """Create a new scavengingtask object."""
    url = f"{BASE_URL}scavengingtask"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_scavengingtask(ref: str, data: dict) -> dict:
    """Update a scavengingtask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_scavengingtask(ref: str) -> dict:
    """Delete a scavengingtask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_scheduledtask(**kwargs) -> dict:
    """Search for scheduledtask objects."""
    url = f"{BASE_URL}scheduledtask"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_scheduledtask(data: dict) -> dict:
    """Create a new scheduledtask object."""
    url = f"{BASE_URL}scheduledtask"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_scheduledtask(ref: str, data: dict) -> dict:
    """Update a scheduledtask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_scheduledtask(ref: str) -> dict:
    """Delete a scheduledtask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_search(**kwargs) -> dict:
    """Search for search objects."""
    url = f"{BASE_URL}search"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_search(data: dict) -> dict:
    """Create a new search object."""
    url = f"{BASE_URL}search"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_search(ref: str, data: dict) -> dict:
    """Update a search object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_search(ref: str) -> dict:
    """Delete a search object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharednetwork(**kwargs) -> dict:
    """Search for sharednetwork objects."""
    url = f"{BASE_URL}sharednetwork"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharednetwork(data: dict) -> dict:
    """Create a new sharednetwork object."""
    url = f"{BASE_URL}sharednetwork"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharednetwork(ref: str, data: dict) -> dict:
    """Update a sharednetwork object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharednetwork(ref: str) -> dict:
    """Delete a sharednetwork object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecord_a(**kwargs) -> dict:
    """Search for sharedrecord:a objects."""
    url = f"{BASE_URL}sharedrecord:a"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecord_a(data: dict) -> dict:
    """Create a new sharedrecord:a object."""
    url = f"{BASE_URL}sharedrecord:a"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecord_a(ref: str, data: dict) -> dict:
    """Update a sharedrecord:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecord_a(ref: str) -> dict:
    """Delete a sharedrecord:a object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecord_aaaa(**kwargs) -> dict:
    """Search for sharedrecord:aaaa objects."""
    url = f"{BASE_URL}sharedrecord:aaaa"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecord_aaaa(data: dict) -> dict:
    """Create a new sharedrecord:aaaa object."""
    url = f"{BASE_URL}sharedrecord:aaaa"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecord_aaaa(ref: str, data: dict) -> dict:
    """Update a sharedrecord:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecord_aaaa(ref: str) -> dict:
    """Delete a sharedrecord:aaaa object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecord_cname(**kwargs) -> dict:
    """Search for sharedrecord:cname objects."""
    url = f"{BASE_URL}sharedrecord:cname"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecord_cname(data: dict) -> dict:
    """Create a new sharedrecord:cname object."""
    url = f"{BASE_URL}sharedrecord:cname"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecord_cname(ref: str, data: dict) -> dict:
    """Update a sharedrecord:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecord_cname(ref: str) -> dict:
    """Delete a sharedrecord:cname object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecord_mx(**kwargs) -> dict:
    """Search for sharedrecord:mx objects."""
    url = f"{BASE_URL}sharedrecord:mx"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecord_mx(data: dict) -> dict:
    """Create a new sharedrecord:mx object."""
    url = f"{BASE_URL}sharedrecord:mx"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecord_mx(ref: str, data: dict) -> dict:
    """Update a sharedrecord:mx object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecord_mx(ref: str) -> dict:
    """Delete a sharedrecord:mx object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecord_srv(**kwargs) -> dict:
    """Search for sharedrecord:srv objects."""
    url = f"{BASE_URL}sharedrecord:srv"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecord_srv(data: dict) -> dict:
    """Create a new sharedrecord:srv object."""
    url = f"{BASE_URL}sharedrecord:srv"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecord_srv(ref: str, data: dict) -> dict:
    """Update a sharedrecord:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecord_srv(ref: str) -> dict:
    """Delete a sharedrecord:srv object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecord_txt(**kwargs) -> dict:
    """Search for sharedrecord:txt objects."""
    url = f"{BASE_URL}sharedrecord:txt"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecord_txt(data: dict) -> dict:
    """Create a new sharedrecord:txt object."""
    url = f"{BASE_URL}sharedrecord:txt"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecord_txt(ref: str, data: dict) -> dict:
    """Update a sharedrecord:txt object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecord_txt(ref: str) -> dict:
    """Delete a sharedrecord:txt object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_sharedrecordgroup(**kwargs) -> dict:
    """Search for sharedrecordgroup objects."""
    url = f"{BASE_URL}sharedrecordgroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_sharedrecordgroup(data: dict) -> dict:
    """Create a new sharedrecordgroup object."""
    url = f"{BASE_URL}sharedrecordgroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_sharedrecordgroup(ref: str, data: dict) -> dict:
    """Update a sharedrecordgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_sharedrecordgroup(ref: str) -> dict:
    """Delete a sharedrecordgroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_smartfolder_children(**kwargs) -> dict:
    """Search for smartfolder:children objects."""
    url = f"{BASE_URL}smartfolder:children"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_smartfolder_children(data: dict) -> dict:
    """Create a new smartfolder:children object."""
    url = f"{BASE_URL}smartfolder:children"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_smartfolder_children(ref: str, data: dict) -> dict:
    """Update a smartfolder:children object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_smartfolder_children(ref: str) -> dict:
    """Delete a smartfolder:children object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_smartfolder_global(**kwargs) -> dict:
    """Search for smartfolder:global objects."""
    url = f"{BASE_URL}smartfolder:global"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_smartfolder_global(data: dict) -> dict:
    """Create a new smartfolder:global object."""
    url = f"{BASE_URL}smartfolder:global"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_smartfolder_global(ref: str, data: dict) -> dict:
    """Update a smartfolder:global object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_smartfolder_global(ref: str) -> dict:
    """Delete a smartfolder:global object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_smartfolder_personal(**kwargs) -> dict:
    """Search for smartfolder:personal objects."""
    url = f"{BASE_URL}smartfolder:personal"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_smartfolder_personal(data: dict) -> dict:
    """Create a new smartfolder:personal object."""
    url = f"{BASE_URL}smartfolder:personal"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_smartfolder_personal(ref: str, data: dict) -> dict:
    """Update a smartfolder:personal object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_smartfolder_personal(ref: str) -> dict:
    """Delete a smartfolder:personal object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_snmpuser(**kwargs) -> dict:
    """Search for snmpuser objects."""
    url = f"{BASE_URL}snmpuser"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_snmpuser(data: dict) -> dict:
    """Create a new snmpuser object."""
    url = f"{BASE_URL}snmpuser"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_snmpuser(ref: str, data: dict) -> dict:
    """Update a snmpuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_snmpuser(ref: str) -> dict:
    """Delete a snmpuser object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_superhost(**kwargs) -> dict:
    """Search for superhost objects."""
    url = f"{BASE_URL}superhost"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_superhost(data: dict) -> dict:
    """Create a new superhost object."""
    url = f"{BASE_URL}superhost"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_superhost(ref: str, data: dict) -> dict:
    """Update a superhost object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_superhost(ref: str) -> dict:
    """Delete a superhost object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_superhostchild(**kwargs) -> dict:
    """Search for superhostchild objects."""
    url = f"{BASE_URL}superhostchild"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_superhostchild(data: dict) -> dict:
    """Create a new superhostchild object."""
    url = f"{BASE_URL}superhostchild"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_superhostchild(ref: str, data: dict) -> dict:
    """Update a superhostchild object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_superhostchild(ref: str) -> dict:
    """Delete a superhostchild object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_syslog_endpoint(**kwargs) -> dict:
    """Search for syslog:endpoint objects."""
    url = f"{BASE_URL}syslog:endpoint"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_syslog_endpoint(data: dict) -> dict:
    """Create a new syslog:endpoint object."""
    url = f"{BASE_URL}syslog:endpoint"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_syslog_endpoint(ref: str, data: dict) -> dict:
    """Update a syslog:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_syslog_endpoint(ref: str) -> dict:
    """Delete a syslog:endpoint object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_tacacsplus_authservice(**kwargs) -> dict:
    """Search for tacacsplus:authservice objects."""
    url = f"{BASE_URL}tacacsplus:authservice"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_tacacsplus_authservice(data: dict) -> dict:
    """Create a new tacacsplus:authservice object."""
    url = f"{BASE_URL}tacacsplus:authservice"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_tacacsplus_authservice(ref: str, data: dict) -> dict:
    """Update a tacacsplus:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_tacacsplus_authservice(ref: str) -> dict:
    """Delete a tacacsplus:authservice object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_taxii(**kwargs) -> dict:
    """Search for taxii objects."""
    url = f"{BASE_URL}taxii"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_taxii(data: dict) -> dict:
    """Create a new taxii object."""
    url = f"{BASE_URL}taxii"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_taxii(ref: str, data: dict) -> dict:
    """Update a taxii object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_taxii(ref: str) -> dict:
    """Delete a taxii object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_tftpfiledir(**kwargs) -> dict:
    """Search for tftpfiledir objects."""
    url = f"{BASE_URL}tftpfiledir"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_tftpfiledir(data: dict) -> dict:
    """Create a new tftpfiledir object."""
    url = f"{BASE_URL}tftpfiledir"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_tftpfiledir(ref: str, data: dict) -> dict:
    """Update a tftpfiledir object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_tftpfiledir(ref: str) -> dict:
    """Delete a tftpfiledir object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatanalytics_analytics_whitelist(**kwargs) -> dict:
    """Search for threatanalytics:analytics_whitelist objects."""
    url = f"{BASE_URL}threatanalytics:analytics_whitelist"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatanalytics_analytics_whitelist(data: dict) -> dict:
    """Create a new threatanalytics:analytics_whitelist object."""
    url = f"{BASE_URL}threatanalytics:analytics_whitelist"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatanalytics_analytics_whitelist(ref: str, data: dict) -> dict:
    """Update a threatanalytics:analytics_whitelist object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatanalytics_analytics_whitelist(ref: str) -> dict:
    """Delete a threatanalytics:analytics_whitelist object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatanalytics_moduleset(**kwargs) -> dict:
    """Search for threatanalytics:moduleset objects."""
    url = f"{BASE_URL}threatanalytics:moduleset"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatanalytics_moduleset(data: dict) -> dict:
    """Create a new threatanalytics:moduleset object."""
    url = f"{BASE_URL}threatanalytics:moduleset"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatanalytics_moduleset(ref: str, data: dict) -> dict:
    """Update a threatanalytics:moduleset object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatanalytics_moduleset(ref: str) -> dict:
    """Delete a threatanalytics:moduleset object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatanalytics_whitelist(**kwargs) -> dict:
    """Search for threatanalytics:whitelist objects."""
    url = f"{BASE_URL}threatanalytics:whitelist"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatanalytics_whitelist(data: dict) -> dict:
    """Create a new threatanalytics:whitelist object."""
    url = f"{BASE_URL}threatanalytics:whitelist"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatanalytics_whitelist(ref: str, data: dict) -> dict:
    """Update a threatanalytics:whitelist object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatanalytics_whitelist(ref: str) -> dict:
    """Delete a threatanalytics:whitelist object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatinsight_cloudclient(**kwargs) -> dict:
    """Search for threatinsight:cloudclient objects."""
    url = f"{BASE_URL}threatinsight:cloudclient"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatinsight_cloudclient(data: dict) -> dict:
    """Create a new threatinsight:cloudclient object."""
    url = f"{BASE_URL}threatinsight:cloudclient"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatinsight_cloudclient(ref: str, data: dict) -> dict:
    """Update a threatinsight:cloudclient object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatinsight_cloudclient(ref: str) -> dict:
    """Delete a threatinsight:cloudclient object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_grid_rule(**kwargs) -> dict:
    """Search for threatprotection:grid:rule objects."""
    url = f"{BASE_URL}threatprotection:grid:rule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_grid_rule(data: dict) -> dict:
    """Create a new threatprotection:grid:rule object."""
    url = f"{BASE_URL}threatprotection:grid:rule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_grid_rule(ref: str, data: dict) -> dict:
    """Update a threatprotection:grid:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_grid_rule(ref: str) -> dict:
    """Delete a threatprotection:grid:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_profile(**kwargs) -> dict:
    """Search for threatprotection:profile objects."""
    url = f"{BASE_URL}threatprotection:profile"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_profile(data: dict) -> dict:
    """Create a new threatprotection:profile object."""
    url = f"{BASE_URL}threatprotection:profile"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_profile(ref: str, data: dict) -> dict:
    """Update a threatprotection:profile object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_profile(ref: str) -> dict:
    """Delete a threatprotection:profile object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_profile_rule(**kwargs) -> dict:
    """Search for threatprotection:profile:rule objects."""
    url = f"{BASE_URL}threatprotection:profile:rule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_profile_rule(data: dict) -> dict:
    """Create a new threatprotection:profile:rule object."""
    url = f"{BASE_URL}threatprotection:profile:rule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_profile_rule(ref: str, data: dict) -> dict:
    """Update a threatprotection:profile:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_profile_rule(ref: str) -> dict:
    """Delete a threatprotection:profile:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_rule(**kwargs) -> dict:
    """Search for threatprotection:rule objects."""
    url = f"{BASE_URL}threatprotection:rule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_rule(data: dict) -> dict:
    """Create a new threatprotection:rule object."""
    url = f"{BASE_URL}threatprotection:rule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_rule(ref: str, data: dict) -> dict:
    """Update a threatprotection:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_rule(ref: str) -> dict:
    """Delete a threatprotection:rule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_rulecategory(**kwargs) -> dict:
    """Search for threatprotection:rulecategory objects."""
    url = f"{BASE_URL}threatprotection:rulecategory"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_rulecategory(data: dict) -> dict:
    """Create a new threatprotection:rulecategory object."""
    url = f"{BASE_URL}threatprotection:rulecategory"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_rulecategory(ref: str, data: dict) -> dict:
    """Update a threatprotection:rulecategory object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_rulecategory(ref: str) -> dict:
    """Delete a threatprotection:rulecategory object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_ruleset(**kwargs) -> dict:
    """Search for threatprotection:ruleset objects."""
    url = f"{BASE_URL}threatprotection:ruleset"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_ruleset(data: dict) -> dict:
    """Create a new threatprotection:ruleset object."""
    url = f"{BASE_URL}threatprotection:ruleset"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_ruleset(ref: str, data: dict) -> dict:
    """Update a threatprotection:ruleset object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_ruleset(ref: str) -> dict:
    """Delete a threatprotection:ruleset object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_ruletemplate(**kwargs) -> dict:
    """Search for threatprotection:ruletemplate objects."""
    url = f"{BASE_URL}threatprotection:ruletemplate"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_ruletemplate(data: dict) -> dict:
    """Create a new threatprotection:ruletemplate object."""
    url = f"{BASE_URL}threatprotection:ruletemplate"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_ruletemplate(ref: str, data: dict) -> dict:
    """Update a threatprotection:ruletemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_ruletemplate(ref: str) -> dict:
    """Delete a threatprotection:ruletemplate object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_threatprotection_statistics(**kwargs) -> dict:
    """Search for threatprotection:statistics objects."""
    url = f"{BASE_URL}threatprotection:statistics"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_threatprotection_statistics(data: dict) -> dict:
    """Create a new threatprotection:statistics object."""
    url = f"{BASE_URL}threatprotection:statistics"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_threatprotection_statistics(ref: str, data: dict) -> dict:
    """Update a threatprotection:statistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_threatprotection_statistics(ref: str) -> dict:
    """Delete a threatprotection:statistics object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_upgradegroup(**kwargs) -> dict:
    """Search for upgradegroup objects."""
    url = f"{BASE_URL}upgradegroup"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_upgradegroup(data: dict) -> dict:
    """Create a new upgradegroup object."""
    url = f"{BASE_URL}upgradegroup"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_upgradegroup(ref: str, data: dict) -> dict:
    """Update a upgradegroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_upgradegroup(ref: str) -> dict:
    """Delete a upgradegroup object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_upgradeschedule(**kwargs) -> dict:
    """Search for upgradeschedule objects."""
    url = f"{BASE_URL}upgradeschedule"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_upgradeschedule(data: dict) -> dict:
    """Create a new upgradeschedule object."""
    url = f"{BASE_URL}upgradeschedule"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_upgradeschedule(ref: str, data: dict) -> dict:
    """Update a upgradeschedule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_upgradeschedule(ref: str) -> dict:
    """Delete a upgradeschedule object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_upgradestatus(**kwargs) -> dict:
    """Search for upgradestatus objects."""
    url = f"{BASE_URL}upgradestatus"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_upgradestatus(data: dict) -> dict:
    """Create a new upgradestatus object."""
    url = f"{BASE_URL}upgradestatus"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_upgradestatus(ref: str, data: dict) -> dict:
    """Update a upgradestatus object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_upgradestatus(ref: str) -> dict:
    """Delete a upgradestatus object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_userprofile(**kwargs) -> dict:
    """Search for userprofile objects."""
    url = f"{BASE_URL}userprofile"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_userprofile(data: dict) -> dict:
    """Create a new userprofile object."""
    url = f"{BASE_URL}userprofile"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_userprofile(ref: str, data: dict) -> dict:
    """Update a userprofile object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_userprofile(ref: str) -> dict:
    """Delete a userprofile object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_vdiscoverytask(**kwargs) -> dict:
    """Search for vdiscoverytask objects."""
    url = f"{BASE_URL}vdiscoverytask"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_vdiscoverytask(data: dict) -> dict:
    """Create a new vdiscoverytask object."""
    url = f"{BASE_URL}vdiscoverytask"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_vdiscoverytask(ref: str, data: dict) -> dict:
    """Update a vdiscoverytask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_vdiscoverytask(ref: str) -> dict:
    """Delete a vdiscoverytask object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_view(**kwargs) -> dict:
    """Search for view objects."""
    url = f"{BASE_URL}view"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_view(data: dict) -> dict:
    """Create a new view object."""
    url = f"{BASE_URL}view"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_view(ref: str, data: dict) -> dict:
    """Update a view object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_view(ref: str) -> dict:
    """Delete a view object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_vlan(**kwargs) -> dict:
    """Search for vlan objects."""
    url = f"{BASE_URL}vlan"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_vlan(data: dict) -> dict:
    """Create a new vlan object."""
    url = f"{BASE_URL}vlan"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_vlan(ref: str, data: dict) -> dict:
    """Update a vlan object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_vlan(ref: str) -> dict:
    """Delete a vlan object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_vlanrange(**kwargs) -> dict:
    """Search for vlanrange objects."""
    url = f"{BASE_URL}vlanrange"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_vlanrange(data: dict) -> dict:
    """Create a new vlanrange object."""
    url = f"{BASE_URL}vlanrange"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_vlanrange(ref: str, data: dict) -> dict:
    """Update a vlanrange object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_vlanrange(ref: str) -> dict:
    """Delete a vlanrange object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_vlanview(**kwargs) -> dict:
    """Search for vlanview objects."""
    url = f"{BASE_URL}vlanview"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_vlanview(data: dict) -> dict:
    """Create a new vlanview object."""
    url = f"{BASE_URL}vlanview"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_vlanview(ref: str, data: dict) -> dict:
    """Update a vlanview object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_vlanview(ref: str) -> dict:
    """Delete a vlanview object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_zone_auth(**kwargs) -> dict:
    """Search for zone_auth objects."""
    url = f"{BASE_URL}zone_auth"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_zone_auth(data: dict) -> dict:
    """Create a new zone_auth object."""
    url = f"{BASE_URL}zone_auth"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_zone_auth(ref: str, data: dict) -> dict:
    """Update a zone_auth object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_zone_auth(ref: str) -> dict:
    """Delete a zone_auth object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_zone_auth_discrepancy(**kwargs) -> dict:
    """Search for zone_auth_discrepancy objects."""
    url = f"{BASE_URL}zone_auth_discrepancy"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_zone_auth_discrepancy(data: dict) -> dict:
    """Create a new zone_auth_discrepancy object."""
    url = f"{BASE_URL}zone_auth_discrepancy"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_zone_auth_discrepancy(ref: str, data: dict) -> dict:
    """Update a zone_auth_discrepancy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_zone_auth_discrepancy(ref: str) -> dict:
    """Delete a zone_auth_discrepancy object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_zone_delegated(**kwargs) -> dict:
    """Search for zone_delegated objects."""
    url = f"{BASE_URL}zone_delegated"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_zone_delegated(data: dict) -> dict:
    """Create a new zone_delegated object."""
    url = f"{BASE_URL}zone_delegated"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_zone_delegated(ref: str, data: dict) -> dict:
    """Update a zone_delegated object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_zone_delegated(ref: str) -> dict:
    """Delete a zone_delegated object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_zone_forward(**kwargs) -> dict:
    """Search for zone_forward objects."""
    url = f"{BASE_URL}zone_forward"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_zone_forward(data: dict) -> dict:
    """Create a new zone_forward object."""
    url = f"{BASE_URL}zone_forward"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_zone_forward(ref: str, data: dict) -> dict:
    """Update a zone_forward object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_zone_forward(ref: str) -> dict:
    """Delete a zone_forward object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_zone_rp(**kwargs) -> dict:
    """Search for zone_rp objects."""
    url = f"{BASE_URL}zone_rp"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_zone_rp(data: dict) -> dict:
    """Create a new zone_rp object."""
    url = f"{BASE_URL}zone_rp"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_zone_rp(ref: str, data: dict) -> dict:
    """Update a zone_rp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_zone_rp(ref: str) -> dict:
    """Delete a zone_rp object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def search_zone_stub(**kwargs) -> dict:
    """Search for zone_stub objects."""
    url = f"{BASE_URL}zone_stub"
    try:
        response = requests.get(url, auth=AUTH, params=kwargs, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def create_zone_stub(data: dict) -> dict:
    """Create a new zone_stub object."""
    url = f"{BASE_URL}zone_stub"
    try:
        response = requests.post(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def update_zone_stub(ref: str, data: dict) -> dict:
    """Update a zone_stub object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.put(url, auth=AUTH, json=data, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}


def delete_zone_stub(ref: str) -> dict:
    """Delete a zone_stub object."""
    url = f"{BASE_URL}{ref}"
    try:
        response = requests.delete(url, auth=AUTH, verify=VERIFY_SSL)
        response.raise_for_status()
        return response.json() if response.content else {"success": True}
    except requests.exceptions.RequestException as e:
        return {"error": str(e)}

